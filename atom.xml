<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://freephenix.github.io/</id>
    <title>半亩房顶</title>
    <updated>2021-04-20T16:38:13.637Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://freephenix.github.io/"/>
    <link rel="self" href="https://freephenix.github.io/atom.xml"/>
    <subtitle>假如我手中有一块小石头</subtitle>
    <logo>https://freephenix.github.io/images/avatar.png</logo>
    <icon>https://freephenix.github.io/favicon.ico</icon>
    <rights>All rights reserved 2021, 半亩房顶</rights>
    <entry>
        <title type="html"><![CDATA[LeetCode-767  重构字符串]]></title>
        <id>https://freephenix.github.io/post/leetcode-767-chong-gou-zi-fu-chuan/</id>
        <link href="https://freephenix.github.io/post/leetcode-767-chong-gou-zi-fu-chuan/">
        </link>
        <updated>2021-04-14T11:31:53.000Z</updated>
        <summary type="html"><![CDATA[<p>刷题使我快乐，满脸开心.jpg</p>
]]></summary>
        <content type="html"><![CDATA[<p>刷题使我快乐，满脸开心.jpg</p>
<!-- more -->
<h1 id="题目">题目</h1>
<p>给定一个字符串S，检查是否能重新排布其中的字母，使得两相邻的字符不同。<br>
若可行，输出任意可行的结果。若不可行，返回空字符串。</p>
<p>示例 1:</p>
<pre><code>输入: S = &quot;aab&quot;
输出: &quot;aba&quot;
</code></pre>
<p>示例 2:</p>
<pre><code>输入: S = &quot;aaab&quot;
输出: &quot;&quot;
</code></pre>
<p>注意:<br>
<code>S 只包含小写字母并且长度在[1, 500]区间内。</code></p>
<h1 id="思路">思路</h1>
<p>本题有两块内容需要思考，一个是如何判断能否得到相邻字符不同的字符串；一个是如何得到这样的字符串<br>
通过一些常识，不难想到，如果字符串中最多的一个字符不大于n+1/2，则就存在这样的一个字符串<br>
这块相对简单<br>
剩下的就是如何得到一个这样的字符串了<br>
首先考虑下极限情况，如果只有一个字符串或者空字符串，则直接返回<br>
如果一共奇数长，有n+1/2个相同的字符，则一定是偶数位置全部摆放最多的这个字符才会得到相邻字符不同的字符串，如果一共偶数长，n/2的最多字符可以在奇数位，也可以在偶数位，那么我们就划分出奇数位和偶数位，间隔插入相同字符即可得到一个这样的字符串。<code>贪心思路</code></p>
<h1 id="代码">代码</h1>
<pre><code>func reorganizeStringMY(S string) string {
    n := len(S)
    if n &lt;= 1 {
        return S
    }

    cnt := [26]int{}
    maxC := 0
    for _, c := range S {
        c -= 'a'
        cnt[c] += 1
        if cnt[c] &gt; maxC {
            maxC = cnt[c]
        }
    }

    if maxC &gt; (n + 1) / 2 {
        return &quot;&quot;
    }

    // 输出
    ret := make([]byte, n)
    oddP, evenP, halfLen := 1, 0, n/2
    for k, v := range cnt {
        // 分奇数位 和 偶数位输出
        c := byte('a' + k)
        for v &gt; 0 &amp;&amp; v &lt;= halfLen &amp;&amp; oddP &lt; n {
            ret[oddP] = c
            v --
            oddP += 2
        }
        for v &gt; 0 &amp;&amp; evenP &lt; n {
            ret[evenP] = c
            v --
            evenP += 2
        }
    }
    return string(ret)
}
</code></pre>
<p>此致，共勉</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[源码杂货铺：channel]]></title>
        <id>https://freephenix.github.io/post/ren-shi-golang-channel/</id>
        <link href="https://freephenix.github.io/post/ren-shi-golang-channel/">
        </link>
        <updated>2021-03-29T09:28:55.000Z</updated>
        <summary type="html"><![CDATA[<p>Do not communicate by sharing memory; instead, share memory by communicating.<br>
这句话相比大家都听过，&quot;不要通过共享内存来通信，而要通过通信来实现内存共享&quot;。<br>
这就是 Go 的并发哲学，它依赖 CSP 模型，基于 channel 实现。</p>
]]></summary>
        <content type="html"><![CDATA[<p>Do not communicate by sharing memory; instead, share memory by communicating.<br>
这句话相比大家都听过，&quot;不要通过共享内存来通信，而要通过通信来实现内存共享&quot;。<br>
这就是 Go 的并发哲学，它依赖 CSP 模型，基于 channel 实现。</p>
<!-- more -->
<h1 id="认识channel">认识channel</h1>
<h2 id="什么是csp模型">什么是CSP模型</h2>
<p>CSP 是 Communicating Sequential Process 的简称，中文可以叫做通信顺序进程，是一种并发编程模型，由 Tony Hoare 于 1977 年提出。简单来说，CSP 就是用<code>通信来实现内存共享</code>的概念模型。</p>
<h2 id="channel-使用">channel 使用</h2>
<p>先来看一个简单的使用</p>
<pre><code>// 注意，channel 是需要使用make来初始化的，如同 slice、map一样
ch := make(chan int, 10)

// 读操作
x &lt;- ch

// 写操作
ch &lt;- x

// 关闭channl
close(ch)
</code></pre>
<ul>
<li>如果我们没有使用make初始化chan的话，那无论读写，都会阻塞g（调用gopark函数），进而造成死锁，fatal error: deadlock</li>
<li>如果我们往一个关闭的channel写，会引发panic</li>
<li>从关闭的channel读，如果缓冲区有内容则还能继续读取到，如果缓存区已经空了，那么会读取到空值</li>
</ul>
<h2 id="channel-底层数据结构">channel 底层数据结构</h2>
<pre><code>type hchan struct {
	qcount   uint           // total data in the queue // 队列中的所有数据数
	dataqsiz uint           // size of the circular queue // 环形队列的大小
	buf      unsafe.Pointer // points to an array of dataqsiz elements // 指向大小为 dataqsiz 的数组
	elemsize uint16
	closed   uint32
	elemtype *_type // element type// 元素类型
	sendx    uint   // send index// 发送索引
	recvx    uint   // receive index// 接受索引
	recvq    waitq  // list of recv waiters 等待列表，即（ &lt;-chan）
	sendq    waitq  // list of send waiters 等待列表，即（ ch&lt;- ）

	// lock protects all fields in hchan, as well as several
	// fields in sudogs blocked on this channel.
	//
	// Do not change another G's status while holding this lock
	// (in particular, do not ready a G), as this can deadlock
	// with stack shrinking.
	// lock 保护了 hchan 的所有字段，以及在此 channel 上阻塞的 sudog 的一些字段
	// 当持有此锁时不应该改变其他 G 的状态（特别的，不 ready 一个 G），因为它会在栈收缩时发生死锁
	lock mutex
}
</code></pre>
<p>主要由一块buffer，发送、接受的指针和列表，还有关闭标识，还有一个互斥锁组成（所以channel是线程安全的）<br>
buffer是有一个环形的队列组成的，但是其实是一个连续内存的数组，用下标作为标记组成的队列，分配的位置就在channel结构体的后面，之后看到源码能看到的，这样用数组访问也方便，性能也会好一些，也是得力于chan不涉及扩缩容</p>
<p><a href="https://go.godbolt.org/">推荐一个golang转汇编在线工具</a>，通过汇编观察，可以看到<br>
1、channel 的构造语句 make(chan int), 对应的是 runtime.makechan 函数<br>
2、发送语句 c &lt;- 1, 对应的是 runtime.chansend1 函数<br>
3、接收语句 x := &lt;- c, 对应的是 runtime.chanrecv1 函数<br>
下面来一个个看下源码</p>
<h2 id="makechan">makeChan</h2>
<pre><code>func makechan64(t *chantype, size int64) *hchan {
    // 其实还是限制int32为最大size
	if int64(int(size)) != size {
		panic(plainError(&quot;makechan: size out of range&quot;))
	}

	return makechan(t, int(size))
}

func makechan(t *chantype, size int) *hchan {
	elem := t.elem

	// compiler checks this but be safe.
	// 最大为65535
	if elem.size &gt;= 1&lt;&lt;16 {
		throw(&quot;makechan: invalid channel element type&quot;)
	}
    // maxAlign  = 8  校验内存对齐
	if hchanSize%maxAlign != 0 || elem.align &gt; maxAlign {
		throw(&quot;makechan: bad alignment&quot;)
	}
    // 校验内存溢出
	mem, overflow := math.MulUintptr(elem.size, uintptr(size))
	if overflow || mem &gt; maxAlloc-hchanSize || size &lt; 0 {
		panic(plainError(&quot;makechan: size out of range&quot;))
	}

	// hchan除了buffer中包含指针之外，不会产生让GC感兴趣的指针
	// 环形队列用的是数组形式分配的，内存块和type都是固定的
	// SudoG's 都被各自的线程引用，所以不会被收集
	// TODO(dvyukov,rlh): Rethink when collector can move allocated objects.
	var c *hchan
	switch {
	case mem == 0:
		// Queue or element size is zero.
		// 队列或元素大小为零
		c = (*hchan)(mallocgc(hchanSize, nil, true))
		// Race detector uses this location for synchronization.
		// 竞争检查使用此位置进行同步
		c.buf = c.raceaddr()
	case elem.ptrdata == 0:
		// Elements do not contain pointers.
		// Allocate hchan and buf in one call.
		// 元素不包含指针，在一个调用中分配 hchan 和 buf
		c = (*hchan)(mallocgc(hchanSize+mem, nil, true))
		c.buf = add(unsafe.Pointer(c), hchanSize)
	default:
		// Elements contain pointers.
		// 元素包含指针，hchan本身使用new，buf使用mallocgc
		c = new(hchan)
		c.buf = mallocgc(mem, elem, true)
	}

	c.elemsize = uint16(elem.size)
	c.elemtype = elem
	c.dataqsiz = uint(size)
	lockInit(&amp;c.lock, lockRankHchan)

	if debugChan {
		print(&quot;makechan: chan=&quot;, c, &quot;; elemsize=&quot;, elem.size, &quot;; dataqsiz=&quot;, size, &quot;\n&quot;)
	}
	return c
}
</code></pre>
<h2 id="chansend">chansend</h2>
<pre><code>// entry point for c &lt;- x from compiled code
//go:nosplit
func chansend1(c *hchan, elem unsafe.Pointer) {
	chansend(c, elem, true, getcallerpc())
}

/*
 * generic single channel send/recv
 * If block is not nil,
 * then the protocol will not
 * sleep but return if it could
 * not complete.
// 泛型单 channel send/recv
// 如果 block 非空，则 protocol 在未完成的情况下会返回而非 sleep
 *
 * sleep can wake up with g.param == nil
 * when a channel involved in the sleep has
 * been closed.  it is easiest to loop and re-run
 * the operation; we'll see that it's now closed.
*/
func chansend(c *hchan, ep unsafe.Pointer, block bool, callerpc uintptr) bool {
	// 当向 nil channel 发送数据时，会调用 gopark
	// 而 gopark 会将当前的 goroutine 休眠，并用过第一个参数的 unlockf 来回调唤醒
	// 但此处传递的参数为 nil，因此向 channel 发送数据的 goroutine 和接收数据的 goroutine 都会阻塞，
	// 进而死锁
	if c == nil {
		if !block {
			return false
		}
		gopark(nil, nil, waitReasonChanSendNilChan, traceEvGoStop, 2)
		throw(&quot;unreachable&quot;)
	}

	if debugChan {
		print(&quot;chansend: chan=&quot;, c, &quot;\n&quot;)
	}

	if raceenabled {
		racereadpc(c.raceaddr(), callerpc, funcPC(chansend))
	}

	// Fast path: check for failed non-blocking operation without acquiring the lock.
	// 快速路径: 在不需要锁的情况下检查失败的非阻塞操作
	// 注意到 channel 不可能从关闭转换为未关闭状态，因此这里的失败条件为：
	// buffer 为空（或没有）时 c.recvq.first 为空，或 buffer 已满
	// 当 c.closed 发生重排在条件之后被读取时，
	// 其实已经隐含了上一个条件检查中 channel 未被关闭。
	if !block &amp;&amp; c.closed == 0 &amp;&amp; full(c) {
		return false
	}

	var t0 int64
	if blockprofilerate &gt; 0 {
		t0 = cputicks()
	}

	lock(&amp;c.lock)

    // 加完锁后，会再次校验一些内容，确保没有问题
	if c.closed != 0 { // 不允许向已经 close 的 channel 发送数据
		unlock(&amp;c.lock)
		panic(plainError(&quot;send on closed channel&quot;))
	}

	// 1. 找到了阻塞在 channel 上的 reciver，直接将数据写入等待的goroutinue的栈上
	if sg := c.recvq.dequeue(); sg != nil {
		// Found a waiting receiver. We pass the value we want to send
		// directly to the receiver, bypassing the channel buffer (if any).
        // 下面有源码，主要是做了memmove内存拷贝和goready唤醒接收的goroutinue
		send(c, sg, ep, func() { unlock(&amp;c.lock) }, 3)
		return true
	}

	// 2. 判断 channel 中缓存区是否仍然有空间剩余
	if c.qcount &lt; c.dataqsiz {
		// Space is available in the channel buffer. Enqueue the element to send.
		// 有空间剩余，加入缓存区
		qp := chanbuf(c, c.sendx)
		if raceenabled {
			racereleaseacquire(qp)
		}
		typedmemmove(c.elemtype, qp, ep)
		c.sendx++
		if c.sendx == c.dataqsiz {
			c.sendx = 0
		}
		c.qcount++
		unlock(&amp;c.lock)
		return true
	}

	if !block {
		unlock(&amp;c.lock)
		return false
	}

	// Block on the channel. Some receiver will complete our operation for us.
	// 3. 阻塞在 channel 上，等待接收方接收数据
	gp := getg()
	mysg := acquireSudog()
	mysg.releasetime = 0
	if t0 != 0 {
		mysg.releasetime = -1
	}
	// No stack splits between assigning elem and enqueuing mysg
	// on gp.waiting where copystack can find it.
	mysg.elem = ep
	mysg.waitlink = nil
	mysg.g = gp
	mysg.isSelect = false
	mysg.c = c
	gp.waiting = mysg
	gp.param = nil
	c.sendq.enqueue(mysg)
	// Signal to anyone trying to shrink our stack that we're about
	// to park on a channel. The window between when this G's status
	// changes and when we set gp.activeStackChans is not safe for
	// stack shrinking.
	atomic.Store8(&amp;gp.parkingOnChan, 1)
    // 将当前的 g 从调度队列移出
	gopark(chanparkcommit, unsafe.Pointer(&amp;c.lock), waitReasonChanSend, traceEvGoBlockSend, 2) 
	// 因为调度器在停止当前 g 的时候会记录运行现场，当恢复阻塞的发送操作时候，会从此处继续开始执行
	// Ensure the value being sent is kept alive until the
	// receiver copies it out. The sudog has a pointer to the
	// stack object, but sudogs aren't considered as roots of the
	// stack tracer.
	KeepAlive(ep)

	// someone woke us up.
	// 被唤醒
	if mysg != gp.waiting {
		throw(&quot;G waiting list is corrupted&quot;)
	}
	gp.waiting = nil
	gp.activeStackChans = false
	closed := !mysg.success
	gp.param = nil
	if mysg.releasetime &gt; 0 {
		blockevent(mysg.releasetime-t0, 2)
	}
	mysg.c = nil
	releaseSudog(mysg)
	if closed {
		// 正常唤醒状态，goroutine 应该包含需要传递的参数，但如果没有唤醒时的参数，且 channel 没有被关闭，则为虚假唤醒
		if c.closed == 0 {
			throw(&quot;chansend: spurious wakeup&quot;)
		}
		panic(plainError(&quot;send on closed channel&quot;))
	}
	return true
}

// send processes a send operation on an empty channel c.
// The value ep sent by the sender is copied to the receiver sg.
// The receiver is then woken up to go on its merry way.
// Channel c must be empty and locked.  send unlocks c with unlockf.
// sg must already be dequeued from c.
// ep must be non-nil and point to the heap or the caller's stack.
func send(c *hchan, sg *sudog, ep unsafe.Pointer, unlockf func(), skip int) {
	if raceenabled {
		if c.dataqsiz == 0 {
			racesync(c, sg)
		} else {
			// Pretend we go through the buffer, even though
			// we copy directly. Note that we need to increment
			// the head/tail locations only when raceenabled.
			qp := chanbuf(c, c.recvx)
			racereleaseacquire(qp)
			racereleaseacquireg(sg.g, qp)
			c.recvx++
			if c.recvx == c.dataqsiz {
				c.recvx = 0
			}
			c.sendx = c.recvx // c.sendx = (c.sendx+1) % c.dataqsiz
		}
	}
	if sg.elem != nil {
		sendDirect(c.elemtype, sg, ep)
		sg.elem = nil
	}
	gp := sg.g
	unlockf()
	gp.param = unsafe.Pointer(sg)
	sg.success = true
	if sg.releasetime != 0 {
		sg.releasetime = cputicks()
	}
	// 复始一个 goroutine，放入调度队列等待被后续调度
	// 第二个参数用于 trace 追踪 ip 寄存器的位置，go runtime 又不希望暴露太多内部的调用，因此记录需要跳过多少 ip
	goready(gp, skip+1)
}

// Sends and receives on unbuffered or empty-buffered channels are the
// only operations where one running goroutine writes to the stack of
// another running goroutine. The GC assumes that stack writes only
// happen when the goroutine is running and are only done by that
// goroutine. Using a write barrier is sufficient to make up for
// violating that assumption, but the write barrier has to work.
// typedmemmove will call bulkBarrierPreWrite, but the target bytes
// are not in the heap, so that will not help. We arrange to call
// memmove and typeBitsBulkBarrier instead.

func sendDirect(t *_type, sg *sudog, src unsafe.Pointer) {
	// src is on our stack, dst is a slot on another stack.

	// Once we read sg.elem out of sg, it will no longer
	// be updated if the destination's stack gets copied (shrunk).
	// So make sure that no preemption points can happen between read &amp; use.
	// 我们必须在一个函数调用中完成 sg.elem 指针的读取，否则当发生栈伸缩时，指针可能失效（被移动了）。
	dst := sg.elem
	// 为了确保发送的数据能够被立刻观察到，需要写屏障支持，执行写屏障，保证代码正确性
	typeBitsBulkBarrier(t, uintptr(dst), uintptr(src), t.size)
	// No need for cgo write barrier checks because dst is always
	// Go memory.
	memmove(dst, src, t.size) // 直接写入 reader 的执行栈！
}
</code></pre>
<p>发送函数，其中有一个地方需要注意，当有等待的接收者时，会直接将内容拷贝进接收者的栈中，就是这么的直接<br>
另外，加锁后，<code>重复校验重要内容</code>值得学习，如校验chan是否关闭等</p>
<h2 id="chanrev">chanrev</h2>
<pre><code>// entry points for &lt;- c from compiled code
//go:nosplit
func chanrecv1(c *hchan, elem unsafe.Pointer) {
	chanrecv(c, elem, true)
}

//go:nosplit
func chanrecv2(c *hchan, elem unsafe.Pointer) (received bool) {
	_, received = chanrecv(c, elem, true)
	return
}

// chanrecv receives on channel c and writes the received data to ep.
// ep may be nil, in which case received data is ignored.
// If block == false and no elements are available, returns (false, false).
// Otherwise, if c is closed, zeros *ep and returns (true, false).
// Otherwise, fills in *ep with an element and returns (true, true).
// A non-nil ep must point to the heap or the caller's stack.
func chanrecv(c *hchan, ep unsafe.Pointer, block bool) (selected, received bool) {
	// raceenabled: don't need to check ep, as it is always on the stack
	// or is new memory allocated by reflect.

	if debugChan {
		print(&quot;chanrecv: chan=&quot;, c, &quot;\n&quot;)
	}

	if c == nil {
		if !block {
			return
		}
		gopark(nil, nil, waitReasonChanReceiveNilChan, traceEvGoStop, 2)
		throw(&quot;unreachable&quot;)
	}

	// Fast path: check for failed non-blocking operation without acquiring the lock.
	// 快速路径: 在不需要锁的情况下检查失败的非阻塞操作
	//
	// 注意到 channel 不能由已关闭转换为未关闭，则
	// 失败的条件是：1. 无 buf 时发送队列为空 2. 有 buf 时，buf 为空
	// 此处的 c.closed 必须在条件判断之后进行验证，
	// 因为指令重排后，如果先判断 c.closed，得出 channel 未关闭，无法判断失败条件中
	// channel 是已关闭还是未关闭（从而需要 atomic 操作）
	if !block &amp;&amp; empty(c) {
		// After observing that the channel is not ready for receiving, we observe whether the
		// channel is closed.
		//
		// Reordering of these checks could lead to incorrect behavior when racing with a close.
		// For example, if the channel was open and not empty, was closed, and then drained,
		// reordered reads could incorrectly indicate &quot;open and empty&quot;. To prevent reordering,
		// we use atomic loads for both checks, and rely on emptying and closing to happen in
		// separate critical sections under the same lock.  This assumption fails when closing
		// an unbuffered channel with a blocked send, but that is an error condition anyway.
		if atomic.Load(&amp;c.closed) == 0 {
			// Because a channel cannot be reopened, the later observation of the channel
			// being not closed implies that it was also not closed at the moment of the
			// first observation. We behave as if we observed the channel at that moment
			// and report that the receive cannot proceed.
			return
		}
		// The channel is irreversibly closed. Re-check whether the channel has any pending data
		// to receive, which could have arrived between the empty and closed checks above.
		// Sequential consistency is also required here, when racing with such a send.
		if empty(c) {
			// The channel is irreversibly closed and empty.
			if raceenabled {
				raceacquire(c.raceaddr())
			}
			if ep != nil {
				typedmemclr(c.elemtype, ep)
			}
			return true, false
		}
	}

	var t0 int64
	if blockprofilerate &gt; 0 {
		t0 = cputicks()
	}

	lock(&amp;c.lock)

	// 1. channel 已经 close，且 channel 中没有数据，则直接返回
	if c.closed != 0 &amp;&amp; c.qcount == 0 {
		if raceenabled {
			raceacquire(c.raceaddr())
		}
		unlock(&amp;c.lock)
		if ep != nil {
			typedmemclr(c.elemtype, ep)
		}
		return true, false
	}
	// 2. 找到发送方，直接接收
	if sg := c.sendq.dequeue(); sg != nil {
		// Found a waiting sender. If buffer is size 0, receive value
		// directly from sender. Otherwise, receive from head of queue
		// and add sender's value to the tail of the queue (both map to
		// the same buffer slot because the queue is full).
		recv(c, sg, ep, func() { unlock(&amp;c.lock) }, 3)
		return true, true
	}
	// 3. channel 的 buf 不空
	if c.qcount &gt; 0 {
		// Receive directly from queue
		qp := chanbuf(c, c.recvx)
		if raceenabled {
			racereleaseacquire(qp)
		}
		if ep != nil {
			typedmemmove(c.elemtype, ep, qp)
		}
		typedmemclr(c.elemtype, qp)
		c.recvx++
		if c.recvx == c.dataqsiz {
			c.recvx = 0
		}
		c.qcount--
		unlock(&amp;c.lock)
		return true, true
	}

	if !block {
		unlock(&amp;c.lock)
		return false, false
	}

	// no sender available: block on this channel.
	// 4. 没有更多的发送方，阻塞 channel
	gp := getg()
	mysg := acquireSudog()
	mysg.releasetime = 0
	if t0 != 0 {
		mysg.releasetime = -1
	}
	// No stack splits between assigning elem and enqueuing mysg
	// on gp.waiting where copystack can find it.
	mysg.elem = ep
	mysg.waitlink = nil
	gp.waiting = mysg
	mysg.g = gp
	mysg.isSelect = false
	mysg.c = c
	gp.param = nil
	c.recvq.enqueue(mysg)
	// Signal to anyone trying to shrink our stack that we're about
	// to park on a channel. The window between when this G's status
	// changes and when we set gp.activeStackChans is not safe for
	// stack shrinking.
	atomic.Store8(&amp;gp.parkingOnChan, 1)
	gopark(chanparkcommit, unsafe.Pointer(&amp;c.lock), waitReasonChanReceive, traceEvGoBlockRecv, 2)

	// someone woke us up
	// 唤醒
	if mysg != gp.waiting {
		throw(&quot;G waiting list is corrupted&quot;)
	}
	gp.waiting = nil
	gp.activeStackChans = false
	if mysg.releasetime &gt; 0 {
		blockevent(mysg.releasetime-t0, 2)
	}
	success := mysg.success
	gp.param = nil
	mysg.c = nil
	releaseSudog(mysg)
	return true, success
}

// recv processes a receive operation on a full channel c.
// There are 2 parts:
// 1) The value sent by the sender sg is put into the channel
//    and the sender is woken up to go on its merry way.
// 2) The value received by the receiver (the current G) is
//    written to ep.
// For synchronous channels, both values are the same.
// For asynchronous channels, the receiver gets its data from
// the channel buffer and the sender's data is put in the
// channel buffer.
// Channel c must be full and locked. recv unlocks c with unlockf.
// sg must already be dequeued from c.
// A non-nil ep must point to the heap or the caller's stack.
func recv(c *hchan, sg *sudog, ep unsafe.Pointer, unlockf func(), skip int) {
	if c.dataqsiz == 0 {
		if raceenabled {
			racesync(c, sg)
		}
		if ep != nil {
			// copy data from sender
			// 直接从对方的栈进行拷贝
			recvDirect(c.elemtype, sg, ep)
		}
	} else {
		// Queue is full. Take the item at the
		// head of the queue. Make the sender enqueue
		// its item at the tail of the queue. Since the
		// queue is full, those are both the same slot.
		// 从缓存队列拷贝
		qp := chanbuf(c, c.recvx)
		if raceenabled {
			racereleaseacquire(qp)
			racereleaseacquireg(sg.g, qp)
		}
		// copy data from queue to receiver
		// 从队列拷贝数据到接收方
		if ep != nil {
			typedmemmove(c.elemtype, ep, qp)
		}
		// copy data from sender to queue
		// 从发送方拷贝数据到队列
		typedmemmove(c.elemtype, qp, sg.elem)
		c.recvx++
		if c.recvx == c.dataqsiz {
			c.recvx = 0
		}
		c.sendx = c.recvx // c.sendx = (c.sendx+1) % c.dataqsiz
	}
	sg.elem = nil
	gp := sg.g
	unlockf()
	gp.param = unsafe.Pointer(sg)
	sg.success = true
	if sg.releasetime != 0 {
		sg.releasetime = cputicks()
	}
	goready(gp, skip+1)
}

func recvDirect(t *_type, sg *sudog, dst unsafe.Pointer) {
	// dst is on our stack or the heap, src is on another stack.
	// The channel is locked, so src will not move during this
	// operation.
	src := sg.elem
	typeBitsBulkBarrier(t, uintptr(dst), uintptr(src), t.size)
	memmove(dst, src, t.size)
}
</code></pre>
<p>接收和发送的代码比较相似，思路之类的都是相似的，大家可以相互借鉴着看</p>
<h2 id="closechan">closechan</h2>
<p>下面来看看关闭操作</p>
<pre><code>func closechan(c *hchan) {
	if c == nil {
        // 不能关闭未初始化的chan
		panic(plainError(&quot;close of nil channel&quot;))
	}

	lock(&amp;c.lock)
	if c.closed != 0 {
		unlock(&amp;c.lock)
        // 不能重复关闭chan，注意细节，panic前释放锁
		panic(plainError(&quot;close of closed channel&quot;))
	}

	if raceenabled {
		callerpc := getcallerpc()
		racewritepc(c.raceaddr(), callerpc, funcPC(closechan))
		racerelease(c.raceaddr())
	}

	c.closed = 1

	var glist gList

	// release all readers
	// 释放所有的读者
	for {
		sg := c.recvq.dequeue()
		if sg == nil {
			break
		}
		if sg.elem != nil {
			typedmemclr(c.elemtype, sg.elem)
			sg.elem = nil
		}
		if sg.releasetime != 0 {
			sg.releasetime = cputicks()
		}
		gp := sg.g
		gp.param = unsafe.Pointer(sg)
		sg.success = false
		if raceenabled {
			raceacquireg(gp, c.raceaddr())
		}
		glist.push(gp)
	}

	// release all writers (they will panic)
	// 释放所有的写者 (panic)
	for {
		sg := c.sendq.dequeue()
		if sg == nil {
			break
		}
		sg.elem = nil
		if sg.releasetime != 0 {
			sg.releasetime = cputicks()
		}
		gp := sg.g
		gp.param = unsafe.Pointer(sg)
		sg.success = false
		if raceenabled {
			raceacquireg(gp, c.raceaddr())
		}
		glist.push(gp)
	}
	unlock(&amp;c.lock)

	// Ready all Gs now that we've dropped the channel lock.
	// 就绪所有的 G 即可释放 channel 锁
    // 并不是在解锁前一个个直接goready去释放，因为这个过程需要与系统交互，成本较高，划重点
	for !glist.empty() {
		gp := glist.pop()
		gp.schedlink = 0
		goready(gp, 3)
	}
}
</code></pre>
<p>释放资源时，没有直接释放资源再解锁，减小了锁占用时间，很值得学习的思路</p>
<h2 id="小结">小结</h2>
<p>Golang 的 channel 将 goroutine 隔离开，并发编程的时候可以将注意力放在 channel 上。在一定程度上，这个和消息队列的解耦功能还是挺像的。有时间多看看源码果真受益匪浅。共勉</p>
<h1 id="引用">引用</h1>
<ul>
<li><a href="https://www.bookstack.cn/read/qcrao-Go-Questions/channel.md">Go语言问题集-channel</a></li>
<li><a href="https://blog.csdn.net/qq_34777600/article/details/81023585?utm_medium=distribute.pc_relevant_t0.none-task-blog-2%7Edefault%7EBlogCommendFromMachineLearnPai2%7Edefault-1.control&amp;dist_request_id=1330144.35573.16182266038558659&amp;depth_1-utm_source=distribute.pc_relevant_t0.none-task-blog-2%7Edefault%7EBlogCommendFromMachineLearnPai2%7Edefault-1.control">Go语言并发与并行--goroutine和channel和deadlock详解</a></li>
<li><a href="https://www.bilibili.com/video/BV1g4411R7p5?from=search&amp;seid=8242326480627556376">#56 channel &amp; select 源码分析 【 Go 夜读 】</a></li>
<li><a href="https://sourcegraph.com/github.com/golang/go@go1.13/-/blob/src/runtime/chan.go#L176">sourcegraph  runtime/chan.go</a></li>
<li><a href="http://legendtkl.com/2017/07/30/understanding-golang-channel/">深入理解 Go Channel</a></li>
<li><a href="https://github.com/golang-design/under-the-hood/blob/master/gosrc/runtime/chan.go">欧长坤大佬注释的go channel源码</a></li>
<li><a href="https://blog.csdn.net/u010853261/article/details/85887948">Golang-gopark函数和goready函数原理分析</a></li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[源码杂货铺：atomic包]]></title>
        <id>https://freephenix.github.io/post/yuan-ma-za-huo-pu-atomic-bao/</id>
        <link href="https://freephenix.github.io/post/yuan-ma-za-huo-pu-atomic-bao/">
        </link>
        <updated>2021-01-29T07:15:18.000Z</updated>
        <summary type="html"><![CDATA[<p>原子(atomic)本意是&quot;不能被进一步分割的最小粒子&quot;，而原子操作(atomic operation)意为&quot;不可中断的一个或一系列操作&quot;。</p>
]]></summary>
        <content type="html"><![CDATA[<p>原子(atomic)本意是&quot;不能被进一步分割的最小粒子&quot;，而原子操作(atomic operation)意为&quot;不可中断的一个或一系列操作&quot;。</p>
<!-- more -->
<h1 id="简介">简介</h1>
<p>golang 中 atomic 类操作最终是使用 assembly 进行 cpu 指令调用实现的，本文只是简单介绍下，至于汇编代码，基本都还给老师了，能捡多少是多少，用到了继续深究</p>
<h1 id="接口定义">接口定义</h1>
<p>主要接口定义在 sync/atomic/doc.go文件中</p>
<pre><code>// Copyright 2011 The Go Authors. All rights reserved.
// Use of this source code is governed by a BSD-style
// license that can be found in the LICENSE file.

// Package atomic provides low-level atomic memory primitives
// useful for implementing synchronization algorithms.
//
// These functions require great care to be used correctly.
// Except for special, low-level applications, synchronization is better
// done with channels or the facilities of the sync package.
// Share memory by communicating;
// don't communicate by sharing memory.
//
// The swap operation, implemented by the SwapT functions, is the atomic
// equivalent of:
//
//	old = *addr
//	*addr = new
//	return old
//
// The compare-and-swap operation, implemented by the CompareAndSwapT
// functions, is the atomic equivalent of:
//
//	if *addr == old {
//		*addr = new
//		return true
//	}
//	return false
//
// The add operation, implemented by the AddT functions, is the atomic
// equivalent of:
//
//	*addr += delta
//	return *addr
//
// The load and store operations, implemented by the LoadT and StoreT
// functions, are the atomic equivalents of &quot;return *addr&quot; and
// &quot;*addr = val&quot;.
//
package atomic

import (
	&quot;unsafe&quot;
)

// BUG(rsc): On x86-32, the 64-bit functions use instructions unavailable before the Pentium MMX.
//
// On non-Linux ARM, the 64-bit functions use instructions unavailable before the ARMv6k core.
//
// On ARM, x86-32, and 32-bit MIPS,
// it is the caller's responsibility to arrange for 64-bit
// alignment of 64-bit words accessed atomically. The first word in a
// variable or in an allocated struct, array, or slice can be relied upon to be
// 64-bit aligned.

// SwapInt32 atomically stores new into *addr and returns the previous *addr value.
func SwapInt32(addr *int32, new int32) (old int32)

// SwapInt64 atomically stores new into *addr and returns the previous *addr value.
func SwapInt64(addr *int64, new int64) (old int64)

// SwapUint32 atomically stores new into *addr and returns the previous *addr value.
func SwapUint32(addr *uint32, new uint32) (old uint32)

// SwapUint64 atomically stores new into *addr and returns the previous *addr value.
func SwapUint64(addr *uint64, new uint64) (old uint64)

// SwapUintptr atomically stores new into *addr and returns the previous *addr value.
func SwapUintptr(addr *uintptr, new uintptr) (old uintptr)

// SwapPointer atomically stores new into *addr and returns the previous *addr value.
func SwapPointer(addr *unsafe.Pointer, new unsafe.Pointer) (old unsafe.Pointer)

// CompareAndSwapInt32 executes the compare-and-swap operation for an int32 value.
func CompareAndSwapInt32(addr *int32, old, new int32) (swapped bool)

// CompareAndSwapInt64 executes the compare-and-swap operation for an int64 value.
func CompareAndSwapInt64(addr *int64, old, new int64) (swapped bool)

// CompareAndSwapUint32 executes the compare-and-swap operation for a uint32 value.
func CompareAndSwapUint32(addr *uint32, old, new uint32) (swapped bool)

// CompareAndSwapUint64 executes the compare-and-swap operation for a uint64 value.
func CompareAndSwapUint64(addr *uint64, old, new uint64) (swapped bool)

// CompareAndSwapUintptr executes the compare-and-swap operation for a uintptr value.
func CompareAndSwapUintptr(addr *uintptr, old, new uintptr) (swapped bool)

// CompareAndSwapPointer executes the compare-and-swap operation for a unsafe.Pointer value.
func CompareAndSwapPointer(addr *unsafe.Pointer, old, new unsafe.Pointer) (swapped bool)

// AddInt32 atomically adds delta to *addr and returns the new value.
func AddInt32(addr *int32, delta int32) (new int32)

// AddUint32 atomically adds delta to *addr and returns the new value.
// To subtract a signed positive constant value c from x, do AddUint32(&amp;x, ^uint32(c-1)).
// In particular, to decrement x, do AddUint32(&amp;x, ^uint32(0)).
func AddUint32(addr *uint32, delta uint32) (new uint32)

// AddInt64 atomically adds delta to *addr and returns the new value.
func AddInt64(addr *int64, delta int64) (new int64)

// AddUint64 atomically adds delta to *addr and returns the new value.
// To subtract a signed positive constant value c from x, do AddUint64(&amp;x, ^uint64(c-1)).
// In particular, to decrement x, do AddUint64(&amp;x, ^uint64(0)).
func AddUint64(addr *uint64, delta uint64) (new uint64)

// AddUintptr atomically adds delta to *addr and returns the new value.
func AddUintptr(addr *uintptr, delta uintptr) (new uintptr)

// LoadInt32 atomically loads *addr.
func LoadInt32(addr *int32) (val int32)

// LoadInt64 atomically loads *addr.
func LoadInt64(addr *int64) (val int64)

// LoadUint32 atomically loads *addr.
func LoadUint32(addr *uint32) (val uint32)

// LoadUint64 atomically loads *addr.
func LoadUint64(addr *uint64) (val uint64)

// LoadUintptr atomically loads *addr.
func LoadUintptr(addr *uintptr) (val uintptr)

// LoadPointer atomically loads *addr.
func LoadPointer(addr *unsafe.Pointer) (val unsafe.Pointer)

// StoreInt32 atomically stores val into *addr.
func StoreInt32(addr *int32, val int32)

// StoreInt64 atomically stores val into *addr.
func StoreInt64(addr *int64, val int64)

// StoreUint32 atomically stores val into *addr.
func StoreUint32(addr *uint32, val uint32)

// StoreUint64 atomically stores val into *addr.
func StoreUint64(addr *uint64, val uint64)

// StoreUintptr atomically stores val into *addr.
func StoreUintptr(addr *uintptr, val uintptr)

// StorePointer atomically stores val into *addr.
func StorePointer(addr *unsafe.Pointer, val unsafe.Pointer)

// Helper for ARM.  Linker will discard on other systems
func panic64() {
	panic(&quot;sync/atomic: broken 64-bit atomic operations (buggy QEMU)&quot;)
}
</code></pre>
<p>一大堆函数，不慌，因为正如开头注释中所说的，主要的就是分为5类函数</p>
<h2 id="swap">Swap</h2>
<p>将参数new的值换到参数addr地址中去，返回地址中存放的旧值</p>
<h2 id="compareandswap">CompareAndSwap</h2>
<p>如果参数addr指向的值与参数old相等，则将addr指向新值参数new，返回true，否则返回false</p>
<h2 id="add">Add</h2>
<p>将参数delta加到参数addr指向的值上，返回加和后新的值</p>
<h2 id="load">Load</h2>
<p>加载参数addr指向的值</p>
<h2 id="store">Store</h2>
<p>存储参数val的值到参数addr指向的地址中</p>
<h1 id="代码实现">代码实现</h1>
<p>那这里只有定义，实现在哪里呢？<br>
sync/atomic/asm.s 中可以看到所有的这些方法，但是这里只是一个JMP，跳转到了 runtime∕internal∕atomic<br>
追到runtime∕internal∕atomic会发现，这里有很多个atomic开头的文件，这里就是针对不同的cpu所需要选择的实际执行的代码了。其中go文件提供接口定义.s文件提供实现，具体的汇编代码分析请各位移步网上各位大神的分析，可以自行百度，或者看看引用文章吧。</p>
<h1 id="value">value</h1>
<p>golang 1.4之后追加了value这个结构，这是atmoic包中提供的一个可以原子保存和加载的结构体</p>
<h2 id="结构体">结构体</h2>
<pre><code>// A Value provides an atomic load and store of a consistently typed value.
// The zero value for a Value returns nil from Load.
// Once Store has been called, a Value must not be copied.
// 一旦被使用过后，就会不能拷贝
// A Value must not be copied after first use.
type Value struct {
	v interface{}
}
</code></pre>
<p>并且，定义了这个结构体，ifaceWords，备注中说明，这个就是空接口的内部实现</p>
<pre><code>// ifaceWords is interface{} internal representation.
type ifaceWords struct {
	typ  unsafe.Pointer
	data unsafe.Pointer
}
</code></pre>
<h2 id="函数">函数</h2>
<p>首先是load方法，顾名思义，取最新store的值返回，没有存过则返回nil</p>
<pre><code>// Load returns the value set by the most recent Store.
// It returns nil if there has been no call to Store for this Value.
func (v *Value) Load() (x interface{}) {
	vp := (*ifaceWords)(unsafe.Pointer(v))
	typ := LoadPointer(&amp;vp.typ)
    // 如果当前的typ是 nil 或者^uintptr(0)，那就证明第一次写入还没有开始，或者还没完成
	if typ == nil || uintptr(typ) == ^uintptr(0) {
		// First store not yet completed.
		return nil
	}
	data := LoadPointer(&amp;vp.data)
	xp := (*ifaceWords)(unsafe.Pointer(&amp;x))
	xp.typ = typ
	xp.data = data
	return
}
</code></pre>
<p>然后是store函数</p>
<pre><code>// Store sets the value of the Value to x.
// All calls to Store for a given Value must use values of the same concrete type.
// Store of an inconsistent type panics, as does Store(nil).
func (v *Value) Store(x interface{}) {
    // 不能存nil
	if x == nil {
		panic(&quot;sync/atomic: store of nil value into Value&quot;)
	}
	vp := (*ifaceWords)(unsafe.Pointer(v))
	xp := (*ifaceWords)(unsafe.Pointer(&amp;x))
	for {
		typ := LoadPointer(&amp;vp.typ)
		if typ == nil {
            // 首次存入
			// Attempt to start first store.
			// Disable preemption so that other goroutines can use
			// active spin wait to wait for completion; and so that
			// GC does not see the fake type accidentally.
            // runtime_procPin这个函数，会防止其他goroutinue抢占
            // 并且使GC不会偶然间发现fake type，应该是指下面的CAS操作中
            // 将vp.type 赋值unsafe.Pointer(^uintptr(0))
			runtime_procPin()
			if !CompareAndSwapPointer(&amp;vp.typ, nil, unsafe.Pointer(^uintptr(0))) {
				runtime_procUnpin()
				continue
			}
			// Complete first store.
			StorePointer(&amp;vp.data, xp.data)
			StorePointer(&amp;vp.typ, xp.typ)
            // 解除锁定
			runtime_procUnpin()
			return
		}
        // 进入则表明有其他请求正在首次写入中
		if uintptr(typ) == ^uintptr(0) {
			// First store in progress. Wait.
			// Since we disable preemption around the first store,
			// we can wait with active spinning.
			continue
		}
		// First store completed. Check type and overwrite data.
        // 校验检查，上一次写入的类型与这一次要写入的类型是否一致，如果不一致则抛出异常
        // 相同再写入数据
		if typ != xp.typ {
			panic(&quot;sync/atomic: store of inconsistently typed value into Value&quot;)
		}
		StorePointer(&amp;vp.data, xp.data)
		return
	}
}
</code></pre>
<h2 id="引申">引申</h2>
<p>这里用的都是原子操作，这由底层硬件支持，而策略上都是乐观锁，而悲观锁则由操作系统的调度器实现。悲观锁应当用来保护一段逻辑，对于一个变量更新的保护，而原子操作通常会更有效率，并且更能利用计算机多核的优势</p>
<h1 id="小结">小结</h1>
<p>到这里基本就结束啦，这里是简单的认识了下atomic包，希望在以后的工作学习中，多多打交道，真的有钻研汇编的那一天。</p>
<h1 id="引用">引用</h1>
<p><a href="https://github.com/cch123/golang-notes/blob/master/atomic.md">曹大golang笔记，atomic 详解</a><br>
<a href="https://mp.weixin.qq.com/s/PQ06eL8kMWoGXodpnyjNcA">详解并发编程基础之原子操作(atomic包)</a><br>
<a href="https://blog.betacat.io/post/golang-atomic-value-exploration/">Go 语言标准库中 atomic.Value 的前世今生</a></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Golang 垃圾回收算法]]></title>
        <id>https://freephenix.github.io/post/golang-la-ji-hui-shou-suan-fa-de-qian-shi-jin-sheng/</id>
        <link href="https://freephenix.github.io/post/golang-la-ji-hui-shou-suan-fa-de-qian-shi-jin-sheng/">
        </link>
        <updated>2021-01-28T22:56:47.000Z</updated>
        <summary type="html"><![CDATA[<p>一提到Golang垃圾回收，三色原理，我们都能脱口而出，那么，它的历程是什么，v1.3、v1.5、v1.8做了什么里程碑式的变革？</p>
]]></summary>
        <content type="html"><![CDATA[<p>一提到Golang垃圾回收，三色原理，我们都能脱口而出，那么，它的历程是什么，v1.3、v1.5、v1.8做了什么里程碑式的变革？</p>
<!-- more -->
<h1 id="golang-垃圾回收里程碑">Golang 垃圾回收里程碑</h1>
<ul>
<li>go v1.3 标记-清除算法</li>
<li>go v1.5 三色标记法</li>
<li>go v1.8 混合写屏障机制</li>
</ul>
<h1 id="golang-v13-标记清除算法">Golang v1.3 标记清除算法</h1>
<ul>
<li>go v1.3之前的标记清除算法，还是大家深深诟病的那个，STW start - mark - sweep - STW done，程序在标记和清理的整个过程中都需要暂停业务程序</li>
<li>gov1.3时做了简单的改进，整体的流程变成了 STW start - mark - STW done - sweep</li>
</ul>
<p>不过我们依然能够发现，STW依然会是一个如鲠在喉的存在，让我们继续看go v1.5之后怎么样了</p>
<h1 id="golang-v15-三色标记法-屏障机制">Golang v1.5 三色标记法 &amp; 屏障机制</h1>
<p>我们先来大致看一下三色标记法是什么步骤</p>
<ul>
<li>第一步：标记启动，对象中默认颜色就是白色，从白色集中向外遍历，遍历到的标位灰色，放入灰色集合</li>
<li>第二步：标记过程，遍历灰色集合，将可达到的对象库存在灰色集合中，遍历完成的灰色标记为黑色，放入黑色集合</li>
<li>第三步：标记终止，灰色集合中全部清空，则可以启动清扫，清扫完成后将所有节点置为白色，等待下一次GC</li>
</ul>
<p>这时候大家肯定会纳闷，STW还有没有，有的话在哪里？屏障机制什么时候启动、又是如何工作的？<br>
下面我们分步骤来看看细节点</p>
<h2 id="标记启动mark-setup-stw">标记启动（Mark Setup - STW）</h2>
<p>首先要开启写屏障（Write Barrier），写屏障保证的是标记过程中数据完整性，因为标记过程中，应用和收集器是并行的。<br>
为了打开写屏障，需要停止所有应用程序的工作，开启过程很快10-30微秒，但是如何保证应用程序能够很好的继续运行呢？那这里就需要提一下，到底在什么时候运行STW？(引申阅读：<a href="https://www.ardanlabs.com/blog/2018/08/scheduling-in-go-part1.html">程序调度系列文章</a>)<br>
答案是在函数调用时，程序调用的时间点是每个goroutinue可以安全暂停的点，当然在并发场景下，即使如此也会有问题，毕竟我们多个goroutinue不可能在同一时间都执行函数调用，这个的解决方案在go 1.14中解决掉了，具体可以参照这个issues: <a href="https://github.com/golang/go/issues/24543">runtime: non-cooperative goroutine preemption</a>，大体方法是向调度器添加非合作抢占来支持安全切换未执行函数调用的goroutinue。</p>
<h2 id="标记过程marking-concurrent">标记过程（Marking - Concurrent）</h2>
<p>一旦开启了写屏障，就进入了标记阶段，收集器先要做的就是占用25%的CPU能力，使用goroutinue工作的收集器需要和现有goroutinue一起使用P和M。简单计算下，就是4个P的程序，其中一个完整的P将分配给收集器（<a href="https://blog.csdn.net/huigan/article/details/106134937">G-P-M</a>）</p>
<p>开始标记时，收集器会从<code>root</code>出发，逐步遍历堆中的内存，完成标记工作，此时，剩余75%的P上依然可以正常运行goroutinue，并发(concurrent)执行。当然事实中不会一直如此顺利，如果有其中一个goroutinue迟迟不能完成收集，怎么办？这个时候，新的内存释放会被放缓，并且收集器会招募应用程序 Goroutines 来协助标记工作，这个叫做标记辅助（Mark Assist）。被抓壮丁的goroutinue占用时间直接取决于堆内存使用的量，当然，对于收集器来说，越少使用标记辅助，就会越好。</p>
<h4 id="引申问题1扫描伊始的root都有哪些">引申问题1：扫描伊始的root都有哪些？</h4>
<ul>
<li>全局变量：程序在编译期就能确定的那些存在于程序整个生命周期的变量。</li>
<li>执行栈：每个 goroutine 都包含自己的执行栈，这些执行栈上包含栈上的变量及指向分配的堆内存区块的指针。这里重点说下栈，golang中每个类型结构里都有一个<code>_type</code>:</li>
</ul>
<pre><code>type _type struct {
    size       uintptr
    ptrdata    uintptr // size of memory prefix holding all pointers
    hash       uint32
    tflag      tflag
    align      uint8
    fieldalign uint8
    kind       uint8
    alg        *typeAlg
    // gcdata stores the GC type data for the garbage collector.
    // If the KindGCProg bit is set in kind, gcdata is a GC program.
    // Otherwise it is a ptrmask bitmap. See mbitmap.go for details.
    gcdata    *byte
    str       nameOff
    ptrToThis typeOff
}
</code></pre>
<p>其中的 gcData 就是为gc做准备的，存储着一个标记指针地址的bitmap，方便gc程序寻找对象。而在运行时，还会有另外一个机制，进一步扫描对象，挖掘引用关系，争取做到可达对象全覆盖： <code>scanobject</code>，它会利用新分配对象时调用的<code>mallocgc</code>函数准备好的<code>heapBitsSetType</code>，方便获取每个内存块中的指针，并扫描出对象。</p>
<ul>
<li>寄存器：寄存器的值可能表示一个指针，参与计算的这些指针可能指向某些赋值器分配的堆内存区块。</li>
</ul>
<h4 id="引申问题2如果内存分配速度超过了标记清除的速度怎么办">引申问题2：如果内存分配速度超过了标记清除的速度怎么办？</h4>
<p>当 GC 触发后，会首先进入并发标记的阶段。并发标记会设置一个标志，并在 mallocgc 调用时进行检查。当存在新的内存分配时，会暂停分配内存过快的那些 goroutine，并将其转去执行一些辅助标记（Mark Assist）的工作，从而达到放缓继续分配、辅助 GC 的标记工作的目的。</p>
<pre><code>func mallocgc(t typ.Type, size uint64){
if enableMarkAssist {
// 进行标记辅助，此时用户代码没有得到执行
(...)
}
// 执行内存分配
(...)
}
</code></pre>
<h2 id="标记终止mark-termination-stw">标记终止（Mark Termination - STW）</h2>
<p>当所有标记完成时，进入终止阶段，在这里阶段，会停止写屏障，清扫垃圾，并准备下一次的垃圾收集。停止标记工作一共历时60-90微秒，其实可以不用STW，但是这个时间延迟，最终决定了还是采用更为简便的STW来完成这个工作。<br>
此时的清扫工作是并发执行的，这个工作本身是不需要受垃圾收集工作制约的。</p>
<h2 id="写屏障机制">写屏障机制</h2>
<p>写屏障主要的目标是解决掉被黑色引用的白色对象被清理的问题，那么v1.5的屏障机制就不难理解：<br>
1、插入写屏障<br>
满足强三色不变性，即被引用对象，会被强制标记为灰色。<br>
2、删除写屏障<br>
满足弱三色不变性，即被删除对象，如果自身为灰色或者白色，会被标记为灰色。<br>
这个机制是否可以优化？看v1.8！</p>
<h2 id="一些补充点">一些补充点</h2>
<h3 id="gc-percentage">GC Percentage</h3>
<p>runtime中有GC Percentage的选项，这个配置数字默认是100表明，需要有新分配100%的堆内存时才会触发下一次GC，请谨慎，设置100会造成延迟</p>
<h3 id="gc-trace">GC Trace</h3>
<p>可以通过包含环境变量 GODEBUG 和 gctrace = 1选项来开启GC trace，这样每次GC时，就会将GC信息写入stderr。运行 GC trace可以告诉我们很多关于应用程序的健康状况和收集器的速度的信息。收集器的运行速度在收集过程中起着重要作用。</p>
<h3 id="pacing">Pacing</h3>
<p>收集器有一个决定GC间隔的算法，即调整GC步长，这个算法依赖于收集的程序信息和程序对堆内存的压力反馈（其实就是分配堆内存速度）</p>
<h3 id="collector-latency-costs">Collector Latency Costs</h3>
<p>GC有两部分的延误程序。<br>
1、CPU占用，减少了CPU，应用程序此时肯定会受到影响。<br>
2、STW，硬生生的暂停和上下文切换成本</p>
<h1 id="golang-v18-混合写屏障机制">Golang v1.8 混合写屏障机制</h1>
<p>在说v1.8改进之前，先说明下三色原理：</p>
<ul>
<li>强三色原理：<strong>黑色对象不会指向白色对象</strong>，所有的黑色对象都只能指向非白色对象就不会出现如上的情况，就不会出现以上的情况，这样所有的白色对象就不会被隐藏，从而保证是安全的。</li>
<li>弱三色原理：<strong>所有黑色对象引用的白色对象，一定能被另外一个灰色对象引用</strong>，有了这个约束后，就算一个黑色对象隐藏了白色对象，那么这个白色对象还是被其他灰色对象调用的。</li>
</ul>
<h2 id="强三色原理">强三色原理</h2>
<p>先来看看这个，应该如何实践这个原理呢？来段伪代码</p>
<pre><code>writePointer(slot, ptr):
    shade(ptr)
    *slot = ptr
</code></pre>
<p>写入时候做shade操作，就是安全的保护指向，简单讲就是比如黑色指向白色时，将白色置灰，不让”黑色对象指向白色对象“，这个自然最彻底，但是如果此时对象在栈上，会比较难实现。作为一个外部的收集器，访问goroutinue内部的栈空间，难度可以想象，会比较复杂，很影响性能不说，为了保证栈上数据的可靠性，还需要借助STW实现对于栈空间的rescan，确保强三色原理满足，这个开销很大，那下面看看弱三色原理</p>
<h2 id="弱三色原理">弱三色原理</h2>
<p>通过上面可以看到，弱三色主要想要做的，是换一种方式，实现对于白对象的保护，先来伪代码</p>
<pre><code>writePointer(slot, ptr):
    shade(*slot)
    if current stack is grey:
        shade(ptr)
    *slot = ptr
</code></pre>
<p>如果新建对象为黑色，而栈又没有写屏障保护，会打破黑色对象引用白色对象的约束，因此混合写保障了弱的三原色约束，这里有个前提就是新建对象都是标为黑色，这样如果栈中指针指向新创建的对象的话也不需要重新扫描栈，因为已经标记为黑色。<br>
用弱三色原理的目的也很简单，这个方式节约了扫描栈空间的成本，极大缩减了STW的时间<br>
至于弱三色原理的正确性证明，感兴趣的可以自行google</p>
<p>v1.8之后，混合写屏障情况如下：<br>
1、GC 在首次执行时，先将栈上的所有对象都标记为黑色。<br>
2、GC 在执行过程中，在栈上新创建的对象，默认被标记为黑色。<br>
3、将被删除的对象标记为灰色。<br>
4、将被添加的对象标记为灰色。</p>
<h4 id="引申问题3什么是写屏障-混合写屏障如何实现">引申问题3：什么是写屏障、混合写屏障，如何实现？</h4>
<p>要讲清楚写屏障，就需要理解三色标记清除算法中的强弱不变性以及赋值器的颜色，理解他们需要一定的抽象思维。写屏障是一个在并发垃圾回收器中才会出现的概念，垃圾回收器的正确性体现在：不应出现对象的丢失，也不应错误的回收还不需要回收的对象。</p>
<p>可以证明，当以下两个条件同时满足时会破坏垃圾回收器的正确性：</p>
<p>条件 1: 赋值器修改对象图，导致某一黑色对象引用白色对象；<br>
条件 2: 从灰色对象出发，到达白色对象的、未经访问过的路径被赋值器破坏。<br>
只要能够避免其中任何一个条件，则不会出现对象丢失的情况，因为：</p>
<p>如果条件 1 被避免，则所有白色对象均被灰色对象引用，没有白色对象会被遗漏；<br>
如果条件 2 被避免，即便白色对象的指针被写入到黑色对象中，但从灰色对象出发，总存在一条没有访问过的路径，从而找到到达白色对象的路径，白色对象最终不会被遗漏。<br>
我们不妨将三色不变性所定义的波面根据这两个条件进行削弱：</p>
<p>当满足原有的三色不变性定义（或上面的两个条件都不满足时）的情况称为<code>强三色不变性</code>（strong tricolor invariant），即<code>强三色原理</code><br>
当赋值器令黑色对象引用白色对象时（满足条件 1 时）的情况称为<code>弱三色不变性</code>（weak tricolor invariant），即<code>弱三色原理</code><br>
当赋值器进一步破坏灰色对象到达白色对象的路径时（进一步满足条件 2 时），即打破弱三色不变性， 也就破坏了回收器的正确性；或者说，在破坏强弱三色不变性时必须引入额外的辅助操作。 弱三色不变形的好处在于：只要存在未访问的能够到达白色对象的路径，就可以将黑色对象指向白色对象。</p>
<p>灰色赋值器的 Dijkstra 插入屏障的基本思想是避免满足条件 1：</p>
<pre><code>// 灰色赋值器 Dijkstra 插入屏障
func DijkstraWritePointer(slot *unsafe.Pointer, ptr unsafe.Pointer){
    shade(ptr)
*slot = ptr
}
</code></pre>
<p><img src="https://freephenix.github.io//post-images/1618216785437.png" alt="" loading="lazy"><br>
Dijkstra 插入屏障的好处在于可以立刻开始并发标记。但存在两个缺点：</p>
<ul>
<li>由于 Dijkstra 插入屏障的“保守”，在一次回收过程中可能会残留一部分对象没有回收成功，只有在下一个回收过程中才会被回收；</li>
<li>在标记阶段中，每次进行指针赋值操作时，都需要引入写屏障，这无疑会增加大量性能开销；为了避免造成性能问题，Go 团队在最终实现时，没有为所有栈上的指针写操作，启用写屏障，而是当发生栈上的写操作时，将栈标记为灰色，但此举产生了灰色赋值器，将会需要标记终止阶段 STW 时对这些栈进行重新扫描。</li>
</ul>
<p>另一种比较经典的写屏障是黑色赋值器的 Yuasa 删除屏障。其基本思想是避免满足条件 2：</p>
<pre><code>// 黑色赋值器 Yuasa 屏障
func YuasaWritePointer(slot *unsafe.Pointer, ptr unsafe.Pointer){
    shade(*slot)
*slot = ptr
}
</code></pre>
<p><img src="https://freephenix.github.io//post-images/1618216815589.png" alt="" loading="lazy"><br>
Yuasa 删除屏障的优势则在于不需要标记结束阶段的重新扫描，结束时候能够准确的回收所有需要回收的白色对象。缺陷是 Yuasa 删除屏障会拦截写操作，进而导致波面的退后，产生“冗余”的扫描：</p>
<p>Go 在 1.8 的时候为了简化 GC 的流程，同时减少标记终止阶段的重扫成本，将 Dijkstra 插入屏障和 Yuasa 删除屏障进行混合，形成混合写屏障。该屏障提出时的基本思想是：对正在被覆盖的对象进行着色，且如果当前栈未扫描完成，则同样对指针进行着色。</p>
<pre><code>// 混合写屏障
func HybridWritePointerSimple(slot *unsafe.Pointer, ptr unsafe.Pointer){
    shade(*slot)
    shade(ptr)
*slot = ptr
}
</code></pre>
<p>在这个实现中，如果无条件对引用双方进行着色，自然结合了 Dijkstra 和 Yuasa 写屏障的优势，但缺点也非常明显，因为着色成本是双倍的，而且编译器需要插入的代码也成倍增加，随之带来的结果就是编译后的二进制文件大小也进一步增加。为了针对写屏障的性能进行优化，Go 1.10 前后，Go 团队随后实现了批量写屏障机制。其基本想法是将需要着色的指针统一写入一个缓存，每当缓存满时统一对缓存中的所有<code>ptr</code>指针进行着色。</p>
<h1 id="gc的触发方式">GC的触发方式</h1>
<p>1、内存分配阈值，阈值=上次 GC 内存分配值 * 内存增长率，其中内存增长率由环境变量 GOGC 设定（GC Percentage），默认值为 100。每次内存分配时，都会先检查当前内存分配是否已经达到阈值，如果已达到阈值，就会触发 GC，即每当内存分配量将要增长一倍时则触发 GC。<br>
2、定时触发，src/runtime/proc.go 文件中的 forcegcperiod 设定触发 GC 的时间间隔，默认值为 2 分钟。<br>
3、手动触发，通过调用 runtime.GC() 方法，触发 GC。</p>
<h1 id="需要关注哪些gc指标">需要关注哪些GC指标</h1>
<p>Go 的 GC 被设计为成比例触发、大部分工作与赋值器并发、不分代、无内存移动且会主动向操作系统归还申请的内存。因此最主要关注的、能够影响赋值器的性能指标有：</p>
<ul>
<li>CPU 利用率：回收算法会在多大程度上拖慢程序？有时候，这个是通过回收占用的 CPU 时间与其它 CPU 时间的百分比来描述的。</li>
<li>GC 停顿时间：回收器会造成多长时间的停顿？目前的 GC 中需要考虑 STW 和 Mark Assist 两个部分可能造成的停顿。</li>
<li>GC 停顿频率：回收器造成的停顿频率是怎样的？目前的 GC 中需要考虑 STW 和 Mark Assist 两个部分可能造成的停顿。</li>
<li>GC 可扩展性：当堆内存变大时，垃圾回收器的性能如何？但大部分的程序可能并不一定关心这个问题。</li>
</ul>
<h1 id="如何调优gc">如何调优GC</h1>
<ul>
<li>控制内存分配的速度，限制 goroutine 的数量，从而提高赋值器对 CPU 的利用率。</li>
<li>减少并复用内存，例如使用 sync.Pool 来复用需要频繁创建临时对象，例如提前分配足够的内存来降低多余的拷贝。</li>
<li>需要时，增大 GOGC 的值，降低 GC 的运行频率。</li>
</ul>
<h1 id="有了gc为什么还会有内存泄露">有了GC，为什么还会有内存泄露？</h1>
<h1 id="演化历程的启发">演化历程的启发</h1>
<ul>
<li>Go 1：串行三色标记清扫</li>
<li>Go 1.3：并行清扫，标记过程需要 STW，停顿时间在约几百毫秒</li>
<li>Go 1.5：并发标记清扫，停顿时间在一百毫秒以内</li>
<li>Go 1.6：使用 bitmap 来记录回收内存的位置，大幅优化垃圾回收器自身消耗的内存，停顿时间在十毫秒以内</li>
<li>Go 1.7：停顿时间控制在两毫秒以内</li>
<li>Go 1.8：混合写屏障，停顿时间在半个毫秒左右</li>
<li>Go 1.9：彻底移除了栈的重扫描过程</li>
<li>Go 1.12：整合了两个阶段的 Mark Termination，但引入了一个严重的 GC Bug 至今未修（见问题 20），尚无该 Bug 对 GC 性能影响的报告</li>
<li>Go 1.13：着手解决向操作系统归还内存的，提出了新的 Scavenger</li>
<li>Go 1.14：替代了仅存活了一个版本的 scavenger，全新的页分配器，优化分配内存过程的速率与现有的扩展性问题，并引入了异步抢占，解决了由于密集循环导致的 STW 时间过长的问题<br>
通观GC的进化历程，我比较关注的有两个重要的点，<br>
一是，通过一步步的不断改进，极大的缩减了STW，这个过程，缺失了每一环，都是有可能无法达到这个局面的。我想说的是，罗马不是一天建成的，我们应该不断积累自己的系统能力，不断完善和强大，为的是有一天，完成那些难以想象的事情。<br>
二是，不要最优，要最简单，这跟go的设计理念相符。我们做项目也是如此，很多时候并不是没有更好的解法，只是，有很多利弊需要权衡，我们要选的，从来都是最适合的。</li>
</ul>
<h1 id="引用">引用</h1>
<p><a href="https://www.cnblogs.com/Delo/articles/12553593.html">【转载】图解Go语言的gc算法——三色标记</a><br>
<a href="https://studygolang.com/articles/27243?fr=sidebar">Golang三色标记、混合写屏障GC模式图文全分析</a><br>
<a href="https://mp.weixin.qq.com/s/gkBg742lpBVRBhTPhOQ7hg">每位 Gopher 都应该了解的 Golang 语言的垃圾回收算法</a><br>
<a href="https://my.oschina.net/u/4579381/blog/4347801">内存管理篇(三)：Go垃圾回收之三色标记算法</a><br>
<a href="https://blog.csdn.net/erlib/article/details/51850912">为Go语言GC正名－20秒到100微妙的演变史</a><br>
<a href="https://blog.csdn.net/bairongdong1/article/details/52216360">02.2跟雨痕看go源码- 并发清理与三色标记</a><br>
<a href="https://blog.csdn.net/waltonhuang/article/details/105550331">三色标记算法 SATB</a><br>
<a href="https://www.ardanlabs.com/blog/2018/12/garbage-collection-in-go-part1-semantics.html">Garbage Collection In Go : Part I - Semantics</a><br>
<a href="https://www.ardanlabs.com/blog/2019/05/garbage-collection-in-go-part2-gctraces.html">Garbage Collection In Go : Part II - GC Traces</a><br>
<a href="https://www.ardanlabs.com/blog/2019/07/garbage-collection-in-go-part3-gcpacing.html">Garbage Collection In Go : Part III - GC Pacing</a><br>
<a href="https://www.jianshu.com/p/310257582232">golang 1.8 gc的演进</a><br>
<a href="https://mp.weixin.qq.com/s/gkBg742lpBVRBhTPhOQ7hg">每位 Gopher 都应该了解的 Golang 语言的垃圾回收算法</a><br>
<a href="https://www.bookstack.cn/read/qcrao-Go-Questions/spilt.9.GC-GC.md">什么是写屏障、混合写屏障，如何实现？</a><br>
<a href="https://www.bookstack.cn/read/qcrao-Go-Questions/spilt.16.GC-GC.md">Go 历史各个版本在 GC 方面的改进？</a></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[源码杂货铺：singleflight]]></title>
        <id>https://freephenix.github.io/post/yuan-ma-za-huo-pu-singleflight/</id>
        <link href="https://freephenix.github.io/post/yuan-ma-za-huo-pu-singleflight/">
        </link>
        <updated>2021-01-21T11:26:34.000Z</updated>
        <summary type="html"><![CDATA[<p>当缓存被击穿，DB不被压垮的一个解法</p>
]]></summary>
        <content type="html"><![CDATA[<p>当缓存被击穿，DB不被压垮的一个解法</p>
<!-- more -->
<h1 id="简介">简介</h1>
<p>这一切都源于一个大接口的优化，当并发升高，很多缓存有可能同时失效时，如何防止DB被打挂，尽可能的保护？重复的请求，重复的资源，如果节约成本？<br>
这一切，有一个解法 -- singleflight</p>
<h1 id="结构体">结构体</h1>
<pre><code>// call is an in-flight or completed singleflight.Do call
// call 表示一个正在执行的或已完成的函数调用，存储调用的接口、报错一些标记等等，
type call struct {
    // 可以看到，singleflight实现中用到了WaitGroup
	wg sync.WaitGroup

	// These fields are written once before the WaitGroup is done
	// and are only read after the WaitGroup is done.
    // 记录结果，在WaitGroup完成之前，只会写入一次，重点在此了
	val interface{}
	err error

	// forgotten indicates whether Forget was called with this call's key
	// while the call was still in flight.
    // 用来标识执行完成之后结果立马删除还是保留在singleflight中
	forgotten bool

	// These fields are read and written with the singleflight
	// mutex held before the WaitGroup is done, and are read but
	// not written after the WaitGroup is done.
    // 这个字段记录执行次数，更新时机是发起请求，waitGroup执行完成‘之前’
	dups  int
    // 用来记录DoChan中需要返回的数据
	chans []chan&lt;- Result
}

// Group represents a class of work and forms a namespace in
// which units of work can be executed with duplicate suppression.
// 用来记录已经存在的对某key的请求和对应的实际请求函数（call）的映射
type Group struct {
	mu sync.Mutex       // protects m 加锁保护m
	m  map[string]*call // lazily initialized  懒加载
}

// Result holds the results of Do, so they can be passed
// on a channel.
// 记录结果，结果值Val，结果报错Err，结果是实际请求还是分享的返回
type Result struct {
	Val    interface{}
	Err    error
	Shared bool
}
</code></pre>
<h1 id="方法">方法</h1>
<h2 id="do">Do</h2>
<pre><code>// Do executes and returns the results of the given function, making
// sure that only one execution is in-flight for a given key at a
// time. If a duplicate comes in, the duplicate caller waits for the
// original to complete and receives the same results.
// The return value shared indicates whether v was given to multiple callers.
// Do执行并返回给定函数的结果，确保在给定时间仅对给定键进行一次执行。
// 如果出现重复，则重复的调用者将等待原始请求接收到相同的结果。
// 共享的返回值指示是否将v分配给多个调用者。
func (g *Group) Do(key string, fn func() (interface{}, error)) (v interface{}, err error, shared bool) {
    // 加锁，主要防止请求重复记录，并非加锁去做请求
	g.mu.Lock()
	if g.m == nil {
		g.m = make(map[string]*call)
	}
    // 校验是否已经完成请求，有则直接返回，shared肯定为true
	if c, ok := g.m[key]; ok {
		c.dups++
		g.mu.Unlock()
		c.wg.Wait()
		return c.val, c.err, true
	}
    // 校验是新的请求，waitGroup加1，存储，解锁
    // 注意，只有真正发起请求时候，才会执行waitGroup加1
	c := new(call)
	c.wg.Add(1)
	g.m[key] = c
	g.mu.Unlock()
    // 实际去请求，返回结果，此时
	g.doCall(c, key, fn)
    // 此时dups应该是恒定为0的？没想明白为什么需要判断，手动执行过Forget时？
	return c.val, c.err, c.dups &gt; 0
}
</code></pre>
<h2 id="dochan">DoChan</h2>
<pre><code>// DoChan is like Do but returns a channel that will receive the
// results when they are ready.
// 整体上与Do函数很类似，只是返回结果变为了chan
func (g *Group) DoChan(key string, fn func() (interface{}, error)) &lt;-chan Result {
	ch := make(chan Result, 1)
	g.mu.Lock()
	if g.m == nil {
		g.m = make(map[string]*call)
	}
	if c, ok := g.m[key]; ok {
		c.dups++
		c.chans = append(c.chans, ch)
		g.mu.Unlock()
		return ch
	}
	c := &amp;call{chans: []chan&lt;- Result{ch}}
	c.wg.Add(1)
	g.m[key] = c
	g.mu.Unlock()

	go g.doCall(c, key, fn)

	return ch
}
</code></pre>
<h2 id="docall">doCall</h2>
<pre><code>// doCall handles the single call for a key.
// 实际执行函数
func (g *Group) doCall(c *call, key string, fn func() (interface{}, error)) {
	c.val, c.err = fn()
	c.wg.Done()
    // 请求完结果，置好waitGroup状态 Done，加锁开始处理后续逻辑
	g.mu.Lock()
    // 默认forgotten为false，会去删除请求好的状态，所以单飞只控制了同一时间相同请求
	if !c.forgotten {
		delete(g.m, key)
	}
    // 将chans中的结果发出去
	for _, ch := range c.chans {
		ch &lt;- Result{c.val, c.err, c.dups &gt; 0}
	}
	g.mu.Unlock()
}
</code></pre>
<h2 id="forget">Forget</h2>
<pre><code>// Forget tells the singleflight to forget about a key.  Future calls
// to Do for this key will call the function rather than waiting for
// an earlier call to complete.
func (g *Group) Forget(key string) {
	g.mu.Lock()
    // 防止重复删除
	if c, ok := g.m[key]; ok {
		c.forgotten = true
	}
    // 删除后，同样的请求，会同时去真实的发起请求
	delete(g.m, key)
	g.mu.Unlock()
}
</code></pre>
<h1 id="总结">总结</h1>
<p>并发是一个双刃剑，节约了时间，但是也带来了很多的问题，不断学习，不断进步，自己要学习的还有很多，共勉！💪💪<br>
希望能用好并发这个工具，解决更多的问题。</p>
<h1 id="引用">引用</h1>
<p><a href="https://blog.csdn.net/yang198907/article/details/108690876">深入理解singleflight</a><br>
<a href="https://www.codeleading.com/article/591682326/">源码分析-singleflight</a><br>
<a href="https://www.jianshu.com/p/ddaec84de653">singleflight源码注释</a><br>
<a href="https://github.com/cch123/golang-notes/blob/master/sync.md">曹大golang笔记 - sync包</a><br>
<a href="https://blog.csdn.net/weixin_43183475/article/details/105093355">singleflight 防缓存击穿 使用及原理</a></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[源码杂货铺：sync.WaitGroup]]></title>
        <id>https://freephenix.github.io/post/yuan-ma-za-huo-pu-syncwaitgroup/</id>
        <link href="https://freephenix.github.io/post/yuan-ma-za-huo-pu-syncwaitgroup/">
        </link>
        <updated>2021-01-10T21:55:59.000Z</updated>
        <summary type="html"><![CDATA[<p>了解一个包的使用，首先从阅读它的注释开始</p>
]]></summary>
        <content type="html"><![CDATA[<p>了解一个包的使用，首先从阅读它的注释开始</p>
<!-- more -->
<h1 id="简介">简介</h1>
<p>sync.WaitGroup相信大家都不陌生，当我们起了多个goroutinue，但是有希望他们同时ready之后再做一些事情的时候，WaitGroup就可以闪亮登场了，举个栗子：</p>
<pre><code>g := sync.WaitGroup{}
// 输入即将要等待goroutinue的数量
g.Add(2)
// func1
go func() {
	// do something
    // 手动确认完成
	g.Done()
}()

// func2
go func() {
	// do something
	g.Done()
}()
// 此时的主goroutinue就会等待 func1 func2完成后再继续往下进行
g.Wait()
</code></pre>
<p>可以说是使用相当方便了，其源码也是非常少的<br>
sync.WaitGroup包注释如下：</p>
<pre><code>// A WaitGroup waits for a collection of goroutines to finish.
// The main goroutine calls Add to set the number of
// goroutines to wait for. Then each of the goroutines
// runs and calls Done when finished. At the same time,
// Wait can be used to block until all goroutines have finished.
//
// A WaitGroup must not be copied after first use.
</code></pre>
<p>包中大体的结构：</p>
<pre><code>type WaitGroup struct {}
func (wg *WaitGroup) state() (statep *uint64, semap *uint32) {}
func (wg *WaitGroup) Add(delta int) {}
func (wg *WaitGroup) Done() {}
func (wg *WaitGroup) Wait() {}
</code></pre>
<p>下面来一个个看其中有什么玄机</p>
<h1 id="waitgroup-struct">WaitGroup struct</h1>
<pre><code>// A WaitGroup waits for a collection of goroutines to finish.
// The main goroutine calls Add to set the number of
// goroutines to wait for. Then each of the goroutines
// runs and calls Done when finished. At the same time,
// Wait can be used to block until all goroutines have finished.
//
// A WaitGroup must not be copied after first use.
type WaitGroup struct {
	noCopy noCopy

	// 64-bit value: high 32 bits are counter, low 32 bits are waiter count.
	// 64-bit atomic operations require 64-bit alignment, but 32-bit
	// compilers do not ensure it. So we allocate 12 bytes and then use
	// the aligned 8 bytes in them as state, and the other 4 as storage
	// for the sema.
	state1 [3]uint32
}
</code></pre>
<h2 id="nocopy">noCopy</h2>
<p>注释中其实简单向我们介绍了用法，这里不多说了。最后的一句话可以关注下，WaitGroup使用后，不可以值拷贝，也就是struct中的noCopy声明实现的效果，这个noCopy的实现和原理大家可以去查阅下资料，go源码中注释和定义如下：</p>
<pre><code>// noCopy may be embedded into structs which must not be copied
// after the first use.
//
// See https://golang.org/issues/8005#issuecomment-190753527
// for details.
type noCopy struct{}
</code></pre>
<p>有时间了可以研究下，这里先简单介绍下，它是Lock包中的一个空结构体，当我们不希望某个struct可以值拷贝时候可以在struct定义里添加这个noCopy结构体，需要注意，编译时候noCopy不会导致报错，可以使用go vet检查</p>
<h2 id="state1">state1</h2>
<p>这个是waitGroup struct中唯一的一个变量了，长度为12个字节，三个变量各用四个字节，从注释中也可以看到其作用的划分，</p>
<ul>
<li>高64位分为两部分，高32位存add、done两个函数操作计数，低32位用于Wait 的计数，存等待中的goroutinue数量</li>
<li>低32位，存储信号量，用于等待和唤醒各个goroutinue，runtime_Semrelease 和 runtime_Semacquire会操作</li>
</ul>
<p>下面开始一个个函数阅读</p>
<h1 id="state函数">state函数</h1>
<pre><code>// state returns pointers to the state and sema fields stored within wg.state1.
func (wg *WaitGroup) state() (statep *uint64, semap *uint32) {
	if uintptr(unsafe.Pointer(&amp;wg.state1))%8 == 0 {
		return (*uint64)(unsafe.Pointer(&amp;wg.state1)), &amp;wg.state1[2]
	} else {
		return (*uint64)(unsafe.Pointer(&amp;wg.state1[1])), &amp;wg.state1[0]
	}
}
</code></pre>
<p>这个函数会返回两个计数器和信号量两个数据，这里的if条件很有意思，这里其实是判断了变量起始地址，除8能除尽，表示的是state1起始地址是内存对齐的，两个计数器数据存在一定关系，所以此时会将前8个字节放在一起，存储两个计数器，这样可以让cpu一次读取，提高效率</p>
<h1 id="add函数-和-done函数">add函数 和 done函数</h1>
<p>done函数本质就是add函数，主要来看下add函数</p>
<pre><code>// Done decrements the WaitGroup counter by one.
func (wg *WaitGroup) Done() {
	wg.Add(-1)
}
</code></pre>
<p>直接上add函数源码，注释中见</p>
<pre><code>// Add adds delta, which may be negative, to the WaitGroup counter.
// add函数可能加正数也可能加负数，负数即done操作
// If the counter becomes zero, all goroutines blocked on Wait are released.
// 如果计数器为0，那所有等待的goroutinue就会被释放
// If the counter goes negative, Add panics.
// 如果计数器为负，调用add会panic，可能是使用有误或者出现并发问题
// Note that calls with a positive delta that occur when the counter is zero
// must happen before a Wait. 
// add只会出现在wait之前，代码中一会儿可以看到判断add和wait未出现并发的校验
// Calls with a negative delta, or calls with a positive delta that start when
// the counter is greater than zero, may happen at any time.
// add 和 done是有可能并发的
// Typically this means the calls to Add should execute before the statement
// creating the goroutine or other event to be waited for.
// add 操作要放在起goroutinue之前和有事情阻塞之前
// If a WaitGroup is reused to wait for several independent sets of events,
// new Add calls must happen after all previous Wait calls have returned.
// 如果要复用一个waitGroup，那么add操作一定要在之前的wait结束之后
// See the WaitGroup example.
func (wg *WaitGroup) Add(delta int) {
    //取计数器和信号量两个参数
	statep, semap := wg.state()
	if race.Enabled {
		// 竞态检查用，可以暂不关注
	}
    // AddUint64 会原子的将传参加到statep上，并且返回一个新值，注意新值，这个下面会用到
    // 原子操作计数器的高4位，即 add 和 done操作的值
	state := atomic.AddUint64(statep, uint64(delta)&lt;&lt;32)
    //v是 add 和 done操作的值
	v := int32(state &gt;&gt; 32)
    //w是 用于wait的值
	w := uint32(state)
	if race.Enabled &amp;&amp; delta &gt; 0 &amp;&amp; v == int32(delta) {
		// 竞态检查用，可以暂不关注
	}
    // v小于零，即注释中说的add 和done的使用出现了问题，panic
	if v &lt; 0 {
		panic(&quot;sync: negative WaitGroup counter&quot;)
	}
    // w != 0 说明有goroutinue走到了wait，delta &gt; 0 &amp;&amp; v == int32(delta) 说明正在调用add
    // 即，wait与add存在并发调用，或者顺序颠倒、使用有误
	if w != 0 &amp;&amp; delta &gt; 0 &amp;&amp; v == int32(delta) {
		panic(&quot;sync: WaitGroup misuse: Add called concurrently with Wait&quot;)
	}
    // v &gt; 0 即调用了add，或者goroutinue们还没有执行完，w == 0表示还没有程序走到wait
    // 直接返回等待后续操作即可
	if v &gt; 0 || w == 0 {
		return
	}
    // 此时，v == 0，即所有goroutinue都已经执行了done，并且w!=0，即有goroutinue开始等待
	// This goroutine has set counter to 0 when waiters &gt; 0.
	// Now there can't be concurrent mutations of state:
	// - Adds must not happen concurrently with Wait,
	// - Wait does not increment waiters if it sees counter == 0.
	// Still do a cheap sanity check to detect WaitGroup misuse.
    // 在v==0的情况下，说明goroutinue都已经执行done完毕了，wait函数就不会有
    // 阻塞效果（一会儿wait代码中能看到），此时statep（原值地址）
    // 不等于state(原子操作后的新值)，只会是又有add参与了执行，致使原值发生改变
    // 本次操作为done，此时接下来要做的wait还没有执行完毕，
    // 又有新的add，说明wait和add并发执行了
	if *statep != state {
		panic(&quot;sync: WaitGroup misuse: Add called concurrently with Wait&quot;)
	}
	// Reset waiters count to 0.
	*statep = 0
    // done均完毕，开始释放信号量
	for ; w != 0; w-- {
		runtime_Semrelease(semap, false, 0)
	}
}
</code></pre>
<h1 id="wait-函数">wait 函数</h1>
<p>注释见</p>
<pre><code>// Wait blocks until the WaitGroup counter is zero.
func (wg *WaitGroup) Wait() {
	statep, semap := wg.state()
	if race.Enabled {
		// 竞态检查用，可以暂不关注
	}
	for 
        // 原子加载statep
		state := atomic.LoadUint64(statep)
		v := int32(state &gt;&gt; 32)
		w := uint32(state)
        // v==0表示done执行完毕，不需要阻塞了
		if v == 0 {
			// Counter is 0, no need to wait.
			if race.Enabled {
				// 竞态检查用，可以暂不关注
			}
			return
		}
		// Increment waiters count.
        // 此时会先比较statep和state是否相等，即有没有其他线程在操作，
        // 如果依然相等则用state+1复制给statep
		if atomic.CompareAndSwapUint64(statep, state, state+1) {
			if race.Enabled &amp;&amp; w == 0 {
				// 竞态检查用，可以暂不关注
			}
            // 在semap上等待信号量
			runtime_Semacquire(semap)
            // add函数中释放信号量时，statep已经被清零了，如果此时不为零说明又有add执行了
			if *statep != 0 {
				panic(&quot;sync: WaitGroup is reused before previous Wait has returned&quot;)
			}
			if race.Enabled {
				// 竞态检查用，可以暂不关注
			}
			return
		}
	}
}
</code></pre>
<h1 id="信号量操作">信号量操作</h1>
<p>信号量相比大家都不陌生，这里贴下两个函数的注释，感兴趣可以深入研究下，这里暂不讨论了<br>
runtime_Semrelease</p>
<pre><code>// Semrelease atomically increments *s and notifies a waiting goroutine
// if one is blocked in Semacquire.
// It is intended as a simple wakeup primitive for use by the synchronization
// library and should not be used directly.
// If handoff is true, pass count directly to the first waiter.
// skipframes is the number of frames to omit during tracing, counting from
// runtime_Semrelease's caller.
func runtime_Semrelease(s *uint32, handoff bool, skipframes int)
</code></pre>
<p>runtime_Semacquire</p>
<pre><code>// Semacquire waits until *s &gt; 0 and then atomically decrements it.
// It is intended as a simple sleep primitive for use by the synchronization
// library and should not be used directly.
func runtime_Semacquire(s *uint32)
</code></pre>
<h1 id="总结">总结</h1>
<p>到这里所有的代码都已经读过了，waitGroup的代码量确实并不多，但是有比较实用，建议多看看，琢磨琢磨，有很多细节值得我们去学习，比如原子做加法、时刻警惕校验异常并发等等<br>
并发问题，确实是一个心智上相对更难理解，但是我们却更加需要特别注意的问题，尤其是我这种从PHP一把梭转到go语言的菜鸟，踩了不少坑。还望大佬们多多指点🙏</p>
<p>下面是总结下waitGroup流程的一个简易图解：<br>
<img src="https://freephenix.github.io//post-images/1610490130883.png" alt="waitGroup" loading="lazy"></p>
<h1 id="引用文章">引用文章</h1>
<ul>
<li><a href="http://yangxikun.com/golang/2020/02/15/golang-sync-waitgroup.html">sync.waitgroup 源码解析</a></li>
<li><a href="https://www.cnblogs.com/rongfengliang/p/14092207.html">golang noCopy 的功能</a></li>
<li><a href="https://github.com/golang/go/issues/8005#issuecomment-190753527">github noCopy特性讨论</a></li>
<li><a href="https://golang.org/cmd/vet/">go vet</a></li>
<li><a href="https://blog.csdn.net/yanluandai1985/article/details/82686486">无锁机制----比较交换CAS Compare And Swap</a></li>
<li><a href="https://blog.csdn.net/qq_19782019/article/details/79746627">操作系统之信号量</a></li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[K8s学习笔记（一）架构介绍]]></title>
        <id>https://freephenix.github.io/post/k8s-xue-xi-bi-ji-yi-jia-gou-jie-shao/</id>
        <link href="https://freephenix.github.io/post/k8s-xue-xi-bi-ji-yi-jia-gou-jie-shao/">
        </link>
        <updated>2020-10-16T02:44:16.000Z</updated>
        <summary type="html"><![CDATA[<p>Kubernetes =&gt; k(8个字符)s</p>
]]></summary>
        <content type="html"><![CDATA[<p>Kubernetes =&gt; k(8个字符)s</p>
<!-- more -->
<h1 id="k8s是什么">k8s是什么</h1>
<p>在不久的将来，你不懂如何操作 Kubernetes 接口，就等于现在的你不懂最普通的 Linux 命令<br>
Kubernetes是容器集群管理系统，是一个开源的平台，可以实现容器集群的自动化部署、自动扩缩容、维护等功能。</p>
<p>通过Kubernetes你可以：</p>
<ul>
<li>快速部署应用</li>
<li>快速扩展应用</li>
<li>无缝对接新的应用功能</li>
<li>节省资源，优化硬件资源的使用<br>
我们的目标是促进完善组件和工具的生态系统，以减轻应用程序在公有云或私有云中运行的负担。</li>
</ul>
<h3 id="kubernetes-特点">Kubernetes 特点</h3>
<ul>
<li>可移植: 支持公有云，私有云，混合云，多重云（multi-cloud）</li>
<li>可扩展: 模块化, 插件化, 可挂载, 可组合</li>
<li>自动化: 自动部署，自动重启，自动复制，自动伸缩/扩展<br>
Kubernetes是Google 2014年创建管理的，是Google 10多年大规模容器管理技术Borg的开源版本。</li>
</ul>
<h3 id="容器优势总结">容器优势总结</h3>
<ul>
<li>快速创建/部署应用：与VM虚拟机相比，容器镜像的创建更加容易。</li>
<li>持续开发、集成和部署：提供可靠且频繁的容器镜像构建/部署，并使用快速和简单的回滚(由于镜像不可变性)。</li>
<li>开发和运行相分离：在build或者release阶段创建容器镜像，使得应用和基础设施解耦。</li>
<li>开发，测试和生产环境一致性：在本地或外网（生产环境）运行的一致性。</li>
<li>云平台或其他操作系统：可以在 Ubuntu、RHEL、 CoreOS、on-prem、Google Container Engine或其它任何环境中运行。</li>
<li>Loosely coupled，分布式，弹性，微服务化：应用程序分为更小的、独立的部件，可以动态部署和管理。</li>
<li>资源隔离</li>
<li>资源利用：更高效</li>
</ul>
<h1 id="k8s组件">k8s组件</h1>
<figure data-type="image" tabindex="1"><img src="https://freephenix.github.io//post-images/1603086936272.png" alt="" loading="lazy"></figure>
<ul>
<li>Apiserver：所有服务访问统一入口，用于暴露Kubernetes API。任何的资源请求/调用操作都是通过kube-apiserver提供的接口进行。</li>
<li>Crontroller Manager：维持副本期望数目。</li>
<li>Scheduler：负责介绍任务， 选择合适的节点进行分配任务</li>
<li>ETCD：键值对数据库，储存K8S集群所有重要信息(持久化)</li>
<li>Kubelet：直接跟容器引擎交互实现容器的生命周期管理</li>
<li>Kube-proxy：负责写入规则至IPTABLES、IPVS实现服务映射访问</li>
<li>CoreDNS：可以为集群中的SVC创建一个域名IP的对应关系解析</li>
<li>DashBoard：给K8s集群提供B/S结构访问体系</li>
<li>Ingress Controller：官方只能实现四层代理，INGRESS可以实现七层代理</li>
<li>Federation：提供一个可以跨集群中心多K8s统一管理功能</li>
<li>Prometheus：提供K8s集群的监控能力</li>
<li>ELK：提供K8s集群日志统一分析介入平台</li>
</ul>
<h1 id="etcd">ETCD</h1>
<figure data-type="image" tabindex="2"><img src="https://freephenix.github.io//post-images/1603086948968.png" alt="" loading="lazy"></figure>
<h3 id="kube-controller-manager">kube-controller-manager</h3>
<p>kube-controller-manager运行管理控制器，它们是集群中处理常规任务的后台线程。逻辑上，每个控制器是一个单独的进程，但为了降低复杂性，它们都被编译成单个二进制文件，并在单个进程中运行。</p>
<blockquote>
<p>这些控制器包括：<br>
节点（Node）控制器。<br>
副本（Replication）控制器：负责维护系统中每个副本中的pod。<br>
端点（Endpoints）控制器：填充Endpoints对象（即连接Services＆Pods）。<br>
Service Account和Token控制器：为新的Namespace 创建默认帐户访问API Token。</p>
</blockquote>
<h3 id="cloud-controller-manager">cloud-controller-manager</h3>
<p>云控制器管理器负责与底层云提供商的平台交互。云控制器管理器是Kubernetes版本1.6中引入的，目前还是Alpha的功能。<br>
云控制器管理器仅运行云提供商特定的（controller loops）控制器循环。可以通过将--cloud-provider flag设置为external启动kube-controller-manager ，来禁用控制器循环。</p>
<blockquote>
<p>cloud-controller-manager 具体功能：<br>
节点（Node）控制器<br>
路由（Route）控制器<br>
Service控制器<br>
卷（Volume）控制器</p>
</blockquote>
<h3 id="kube-scheduler">kube-scheduler</h3>
<p>kube-scheduler 监视新创建没有分配到Node的Pod，为Pod选择一个Node。</p>
<h1 id="节点node组件">节点（Node）组件</h1>
<p>节点组件运行在Node，提供Kubernetes运行时环境，以及维护Pod。</p>
<h3 id="kubelet">kubelet</h3>
<p>kubelet是主要的节点代理，它会监视已分配给节点的pod，具体功能：</p>
<ul>
<li>安装Pod所需的volume。</li>
<li>下载Pod的Secrets。</li>
<li>Pod中运行的 docker（或experimentally，rkt）容器。</li>
<li>定期执行容器健康检查。</li>
<li>Reports the status of the pod back to the rest of the system, by creating a mirror pod if necessary.</li>
<li>Reports the status of the node back to the rest of the system.</li>
</ul>
<h3 id="kube-proxy">kube-proxy</h3>
<p>kube-proxy通过在主机上维护网络规则并执行连接转发来实现Kubernetes服务抽象。</p>
<h3 id="docker">docker</h3>
<p>docker用于运行容器。</p>
<h3 id="rkt">RKT</h3>
<p>rkt运行容器，作为docker工具的替代方案。</p>
<h3 id="supervisord">supervisord</h3>
<p>supervisord是一个轻量级的监控系统，用于保障kubelet和docker运行。</p>
<h3 id="fluentd">fluentd</h3>
<p>fluentd是一个守护进程，可提供cluster-level logging.。</p>
<h1 id="引用">引用</h1>
<ul>
<li><img src="https://www.kubernetes.org.cn/" alt="k8s中文社区" loading="lazy"></li>
<li><img src="http://docs.kubernetes.org.cn/" alt="k8s中文文档" loading="lazy"></li>
<li><img src="http://dockone.io/article/1448" alt="k8s为什么很重要" loading="lazy"></li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[GORM学习(一) 初识]]></title>
        <id>https://freephenix.github.io/post/gorm-xue-xi-yi-curd/</id>
        <link href="https://freephenix.github.io/post/gorm-xue-xi-yi-curd/">
        </link>
        <updated>2020-09-17T01:54:26.000Z</updated>
        <summary type="html"><![CDATA[<p>初识GORM</p>
]]></summary>
        <content type="html"><![CDATA[<p>初识GORM</p>
<!-- more -->
<h1 id="什么是orm">什么是ORM</h1>
<p>Object-Relationl Mapping， 它的作用是映射数据库和对象之间的关系，方便我们在实现数据库操作的时候不用去写复杂的sql语句，把对数据库的操作上升到对于对象的操作。</p>
<h1 id="什么是gorm">什么是GORM</h1>
<p>顾名思义，gorm = go + orm，即基于go语言实现的orm库<br>
v1版本github地址：https://github.com/jinzhu/gorm<br>
v2版本github地址：https://github.com/go-gorm/gorm<br>
官网地址：https://gorm.io/zh_CN/<br>
官方文档地址：https://gorm.io/zh_CN/docs/index.html</p>
<h1 id="gorm-结构标记">GORM 结构标记</h1>
<blockquote>
<p>安装过程以及简单的curd，大家看下官方文档即可，使用起来还是比较方便的，也与其他的orm没有大的区别，这里想先提下gorm中的结构标记</p>
</blockquote>
<p>大家都知道，orm是完成了数据表和对象之间的映射，而这个对象的结构、字段等等，都是需要我们在程序中定义出来的，但是gorm中有一点与以往不同，gorm中提供了功能强大丰富的结构标记</p>
<pre><code>type User struct {
    ID         int64
    Number     int64
    Name       string
    ImgURL    string
    CreatedAt  time.Time
    UpdatedAt  time.Time
}
</code></pre>
<p>再辅以<code>AutoMigrate</code>方法，就可以让我们直接通过代码生成对应的数据表，这一<code>逆向操作</code>起初应该是不甚习惯的，毕竟大多数情况下来说，我们的习惯还是先设计出数据表，然后给映射出对象，再给这个对象添加功能、使用对象完成逻辑实现等</p>
<p>但其实我们自己也经常会遇到这样的情况，设计出数据表后，往往在实际开发业务时候，或者在之后的迭代中，会发现我们的数据表需要一点点的打补丁，添加或者修改字段等等，其实道理也很明白，一时的设计，很难满足未出现的需求，而需求是不断更新迭代的，设计自然需要迎合和适应。</p>
<p>基于这个观点，我认为这个逆向的过程确实是一个值得思考的点，当然这里也会有很多问题：比如增加信息同步成本、增加高效协作难度等等</p>
<p>好，回归正题，我们先来看下部分常用的标签们：</p>
<pre><code>type User struct {
    ID         int64        `gorm:&quot;type:bigint(20);primary_key;autoIncrement&quot;`
    Number     int64        `gorm:&quot;&lt;-:create;index:idx_number&quot;`
    Name       string       
    ImgURL     string       `gorm:&quot;column:name&quot;`
    CreatedAt  time.Time    `gorm:&quot;autoCreateTime&quot;`
    UpdatedAt  time.Time    `gorm:&quot;autoUpdateTime&quot;`
}
</code></pre>
<ul>
<li>primary_key 指定主键，可以不指定在id字段上</li>
<li>type 指定字段类型</li>
<li>autoIncrement 自增</li>
<li>&lt;-:false，跟 chan 相似左箭头为写，右箭头为读， create代表只能创建时写</li>
<li>index 创建索引，冒号后跟索引名称，可为空，相同非空名称的索引会创建联合索引</li>
<li>autoCreateTime 自动s为单位填充创建时间</li>
<li>autoUpdateTime 自动s为单位填充更新时间</li>
<li>column 这个类似json标签，用于字段映射时，无法直接通过首字母大小写的转化和驼峰和下划线互转等操作，完成字段和数据库列名直接映射上的情况，直接指定列名</li>
</ul>
<p>对于其他标签，以及一些比较高级的<a href="https://gorm.io/zh_CN/docs/indexes.html">索引</a>、<a href="https://gorm.io/zh_CN/docs/constraints.html">约束</a>、<a href="https://gorm.io/zh_CN/docs/composite_primary_key.html">复合主键</a>标签，大家可以参看官方文档</p>
<h1 id="gorm-v2-更新内容摘要">GORM v2 更新内容摘要</h1>
<h3 id="context-支持">Context 支持</h3>
<p>增加原生支持ctx</p>
<h3 id="create-支持批量创建">create 支持批量创建</h3>
<ul>
<li>生成一条语句来进行插入（难不成还遍历？）</li>
<li>数据有关联的将会调用upsert</li>
<li>批量插入依然会调用<code>钩子</code>方法</li>
</ul>
<h3 id="upsert">upsert</h3>
<p><code>clause.OnConflict</code> 将会提供upsert支持</p>
<pre><code>import &quot;gorm.io/gorm/clause&quot;

DB.Clauses(clause.OnConflict{DoNothing: true}).Create(&amp;users)
</code></pre>
<h3 id="支持嵌套事务">支持嵌套事务</h3>
<h3 id="savepointrollbackto">SavePoint，RollbackTo</h3>
<p>事务操作将会更加丰富</p>
<h3 id="字段权限-和-更新-创建时间追踪">字段权限 和 更新、创建时间追踪</h3>
<p>还记得上面标签提高的<code>&lt;-:create</code>和<code>gorm:&quot;autoCreateTime</code>等吗，对的，v2.0开始支持</p>
<h3 id="prometheus">Prometheus</h3>
<p>增加Prometheus插件支持来收集 DBStats 和用户自定义指标<br>
<a href="https://gorm.io/zh_CN/docs/prometheus.html">Prometheus插件简介</a></p>
<h3 id="tablename-不再允许动态表名">tableName 不再允许动态表名</h3>
<pre><code>func (User) TableName() string {
  return &quot;t_user&quot;
}
</code></pre>
<p>当然，以后使用动态表名可以使用<code>scope</code>实现</p>
<pre><code>func UserTable(u *User) func(*gorm.DB) *gorm.DB {
  return func(db *gorm.DB) *gorm.DB {
    return db.Table(&quot;user_&quot; + u.Role)
  }
}

DB.Scopes(UserTable(&amp;user)).Create(&amp;user)
</code></pre>
<h3 id="migrator迁移将会更加强大">Migrator（迁移）将会更加强大</h3>
<p><a href="https://gorm.io/zh_CN/docs/migration.html">gorm 迁移</a></p>
<h1 id="总结">总结</h1>
<p>总的来说，gorm是一个很遍历，足够强大，值得深挖源码，深入学习的orm，燥起来！</p>
<h1 id="引用">引用</h1>
<p><a href="https://gorm.io/zh_CN/docs/models.html">gorm 声明模型</a><br>
<a href="https://gorm.io/zh_CN/docs/indexes.html">gorm 索引标签</a><br>
<a href="https://gorm.io/zh_CN/docs/constraints.html">gorm 约束标签</a><br>
<a href="https://gorm.io/zh_CN/docs/composite_primary_key.html">gorm 复合主键标签</a><br>
<a href="https://gorm.io/zh_CN/docs/migration.html">gorm 迁移</a></p>
<hr>
<p>欢迎大家关注我的公众号，一起交流<br>
<img src="https://freephenix.github.io//post-images/1601453523798.jpeg" alt="" loading="lazy"></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[DDD 学习笔记（四）]]></title>
        <id>https://freephenix.github.io/post/ddd-xue-xi-bi-ji-si/</id>
        <link href="https://freephenix.github.io/post/ddd-xue-xi-bi-ji-si/">
        </link>
        <updated>2020-09-11T05:14:26.000Z</updated>
        <summary type="html"><![CDATA[<p>DDD 分层架构、整洁架构、六边形架构都是以领域模型 为核心，实行分层架构，内部核心业务逻辑与外部应用、资源隔离并解耦。</p>
]]></summary>
        <content type="html"><![CDATA[<p>DDD 分层架构、整洁架构、六边形架构都是以领域模型 为核心，实行分层架构，内部核心业务逻辑与外部应用、资源隔离并解耦。</p>
<!-- more -->
<h1 id="架构种类">架构种类</h1>
<h3 id="整洁架构">整洁架构</h3>
<p>又名“洋葱架构”，越往里，越核心，越少依赖<br>
<img src="https://freephenix.github.io//post-images/1599801911257.png" alt="" loading="lazy"><br>
领域模型实现领域内核心业务逻辑<br>
领域服务实现涉及多个实体的复杂业务逻辑<br>
应用服务实现与用户操作相关的服务组合与编排<br>
用户界面主要提供适配的能力</p>
<h3 id="六边形架构">六边形架构</h3>
<p>又名“端口适配器架构”，应用通过端口与外部进行交互<br>
<img src="https://freephenix.github.io//post-images/1599806278259.png" alt="" loading="lazy"><br>
红圈内的六边形完成核心业务逻辑<br>
外部的六边形完成外部依赖、驱动和基础资源等的交互</p>
<h3 id="总结">总结</h3>
<p>结合上讲中提到的DDD 分层架构，对于这三种进行一个对比<br>
<img src="https://freephenix.github.io//post-images/1599806564666.png" alt="" loading="lazy"><br>
红圈中均为最核心的逻辑，而三者都考虑到了前端需求会经常改变，但是领域模型并不会经常改变</p>
<p>DDD 分层架构、整洁架构、六边形架构都是以领域模型 为核心，实行分层架构，内部核心业务逻辑与外部应用、资源隔离并解耦。</p>
<hr>
<p>欢迎大家关注我的公众号<br>
<img src="https://freephenix.github.io//post-images/1599807261669.jpeg" alt="" loading="lazy"></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[DDD 学习笔记（二）基础概念]]></title>
        <id>https://freephenix.github.io/post/ddd-xue-xi-bi-ji-er-ji-chu-gai-nian/</id>
        <link href="https://freephenix.github.io/post/ddd-xue-xi-bi-ji-er-ji-chu-gai-nian/">
        </link>
        <updated>2020-09-08T12:50:30.000Z</updated>
        <summary type="html"><![CDATA[<p>DDD 基础概念</p>
]]></summary>
        <content type="html"><![CDATA[<p>DDD 基础概念</p>
<!-- more -->
<h1 id="概念">概念</h1>
<h3 id="领域-与-子域">领域 与 子域</h3>
<ul>
<li>DDD 的领域就是这个边界内要解决的业务问题域。</li>
<li>我们把划分出来的多个子领域称为子域，每个子域对应一个更小的问题域或更小的业务范围。</li>
</ul>
<h3 id="核心域-通用域和支撑域">核心域、通用域和支撑域</h3>
<p>这三类为三种不同的子域，不同系统中，核心域不尽相同，需要公司根据自身业务，选定核心域等，完成划分</p>
<ul>
<li>决定产品和公司核心竞争力的子域是核心域</li>
<li>同时被多个子域使用的通用功能子域是通用域</li>
<li>必需的，但既不包含决定产品和公司核心竞争力的功能，也不包含通用功能的子域，它就是支撑域。</li>
</ul>
<h3 id="限界上下文">限界上下文</h3>
<p>通用语言定义上下文含义，限界上下文则定义领域边界</p>
<h5 id="为什么要提出限界上下文的概念">为什么要提出限界上下文的概念？</h5>
<p>在事件风暴过程中，通过团队交流达成共识的，能够简单、清晰、准确描述业务涵义和规则的语言就是通用语言。通用语言可以直接反应在代码中。<br>
1、但是通用语言难以完成领域的划分，进而确定微服务的划分，解决微服务想要解决的问题<br>
2、通用语言在不同上下文中会有不同的含义，引入限界上下文，可以提供上下文环境，消除歧义。</p>
<h5 id="限界上下文在微服务设计中的作用和意义是什么">限界上下文在微服务设计中的作用和意义是什么？</h5>
<p>1、封装通用语言和领域对象，让大家明白功能应该放于哪个领域。<br>
2、划分领域边界，确定限界上下文，消除歧义。</p>
<h3 id="实体-和-值对象">实体 和 值对象</h3>
<p>实体和值对象是组成领域模型的基础单元。</p>
<h5 id="实体">实体</h5>
<ul>
<li>有 ID 标识，聚合内唯一</li>
<li>这些实体类通常采用充血模型</li>
<li>依附于聚合根，其生命周期由聚合根管理</li>
<li>与数据库持久化对象不一定是 一对一的关系</li>
<li>可以引用聚合内的聚合根、实体和值对象</li>
</ul>
<h5 id="值对象">值对象</h5>
<ul>
<li>不可变，无生命周期，用完即扔</li>
<li>通过对象属性值来识别的对象，它将多个相关属性组合为一个概念整体</li>
<li>简单来说，值对象本质上就是一个集。由若干个用于描述目的、具有整体概念和不可修改的属性组成</li>
<li>值对象尽量只引用值对象</li>
</ul>
<blockquote>
<p>DDD 提倡从领域模型设计出发，而不是先设计数据模型</p>
</blockquote>
<h3 id="聚合和聚合根">聚合和聚合根</h3>
<h5 id="聚合">聚合</h5>
<ul>
<li>高内聚、低耦合，它是领域模型中最底层的边界，可以作为拆分微服务的最小单位</li>
<li>聚合让一些列实体和值对象协同工作，确保这些领域对象在实现共同业务时，保持一致性</li>
<li>聚合之间的边界是微服务内天然的逻辑边界</li>
</ul>
<h5 id="聚合根">聚合根</h5>
<ul>
<li>聚合根是实体，有实体的特点，具有全局唯一标识，有独立的生命周期</li>
<li>聚合相对于组织的话，聚合根则为这个组织的管理者，协调实体和值对象，完成功能</li>
<li>一个聚合只有一个聚合根，聚合根在聚合内对实体和值对象采用直接对象引用的方式进行组织 和协调</li>
<li>聚合根与聚合根之间通过 ID 关联的方式实现聚合之间的协同</li>
<li>其他聚合调用其他聚合中的对象，均需要通过聚合根调用</li>
</ul>
<h6 id="如何设计聚合">如何设计聚合</h6>
<p>1、根据事件风暴，梳理出实体和值对象<br>
2、从实体中选出聚合根<br>
3、根据单一职责，以聚合根为核心，划分出所有的相关实体和值对象（整理出依赖模型，确定依赖关系），此为一个聚合<br>
4、将多个聚合根据业务语义和上下文划分到同一个限界上下文中，结束聚合设计</p>
<hr>
<p>欢迎大家关注公众号一起交流<br>
<img src="https://freephenix.github.io//post-images/1599790602118.jpeg" alt="" loading="lazy"></p>
]]></content>
    </entry>
</feed>